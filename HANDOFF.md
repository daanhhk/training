# HANDOFF — project training (FTP Coach web-app)

Bron van waarheid voor de projectstand. Conventies + architectuur-detail + code-invarianten staan in CLAUDE.md (auto-load Claude Code); deze HANDOFF = STAND, aanpak, roadmap + engine-referentie.

## STAND (leidend)
INTERACTIEVE HtmlService web-app. /dev:
https://script.google.com/macros/s/AKfycbz51mSRp2LYEIWFPJLmahX14_40w5c85UEDcjCSIW-J/dev
- **COVERAGE↔VOLUME-COHERENTIE + VO2MAX/CONDITIE-PROFIELEN + CLEANUP GESHIPT (selftest 659/0).** *Track 1 (cov-gate, 4b1ad35)*: in Base ≤ `BASE_POLAR_VOL_U0` krijgt vo2 GEEN coverage-boost (`goalPickIntent_` `vo2GateBase`) — een anaerobic-gat wordt bij laag volume NIET via quality-injectie gedekt (volume stuurt vo2, niet de dekking); >U0 + Build/Peak ongewijzigd; drempel/sweetspot onaangeroerd. *Track 2 (4b1ad35)*: eigen `PROFILES.vo2max` (drempel 0.35/sweetspot 0.25/vo2 0.40; `volumeResponse` 0.02/0.08) + `PROFILES.conditie` (sweetspot 0.45/drempel 0.35/vo2 0.20; 0.04/0.38), elk gecapt < hun #1 Base-intent + week-plaatsing-velden; `projectieKey` weggelaten (ftp-patroon — `GOAL_PROFILES_` blijft girona-only). `profileForDoel_`: VO2max→vo2max, Conditie→conditie (waren default klim). *Track 3 cleanup*: rijdertype-as 'Allrounder'→'All-rounder' (76c2a08); dode Vorm-A-client-fns `drawNiveauChart`/`renderNiveauHead_`/`setNiveauWindow`/`niveauReeksDelta_` weg (76c2a08; `niveauReeksWindow_`+`niveauWindow` nu óók orphan → future); vestigiale `eventDate`-param van `assignWorkouts` weg (1263ab7; geen test raakt 't → positie-shift per inspectie geverifieerd). +`testCovGateBase_` (6) +`testProfielenVo2maxConditie_` (8) + mapping-reconcile (+2) → 659. **VERIFICATIE OPEN:** /dev — Vorm/Niveau-tabs renderen nog (client-fn-removal is niet gate-gedekt).
- **VOLUME-ADAPTIEVE BASE-KWALITEIT — PASS 1 GESHIPT (selftest 643/0).** Additieve `volumeModulatie(V, fase, doelProfiel)`-term op de RAUWE intent-sort-scores (geen clamp/renorm), gelaagd boven `faseModulatie`. Pass 1 = ALLEEN de vo2-key in Base; drempel/sweetspot Δ=0; Build/Peak Δ=0 (byte-identiek). vo2 ramt vanaf `BASE_POLAR_VOL_U0` (9 weekuren), gecapt < de #1 Base-intent via per-profiel `volumeResponse {vo2Slope,vo2Cap}`: klim 0.03/0.15 (vo2 0.25 passeert sweetspot 0.35, blijft < drempel 0.45 → wordt #2, nooit #1); ftp 0.04/0.38 (vo2 0.10 ver onder sweetspot 0.45). Geen suppressie-floor nodig (vo2 al 3e bij lage V, beide profielen). Base-quality loopt nu via dezelfde `goalWorkout_`-keuze als Build/Peak (was hardcoded sweet_spot) + weekvolume `weekV = Σ(weekDays||days).minuten/60` (in `allocateQualityWeek_`). Commits: term `de31489`, wiring `6c1c64d`. +`testVolumeModulatie_` (10 asserts) + Base-reconcile (`alloc Base quality=sweet_spot` → types+arch/drempel-led/geen-herhaling, +2) → 643. **GEFASEERD:** pass 2 = Build/Peak-consument; pass 3 = volume-gevoelige `kwaliteitPerWeek` + Z3/Z4-middle-taper (volle curve); pass 4 = kalibratie slope/cap + crit-profiel om de doel-slope te valideren. **VERIFICATIE OPEN:** /dev — een hoog-volume Base-week toont vo2 in de kwaliteitsmix.
- **ITEM 3 + ARCHETYPE-DIVERSITEIT GESHIPT (selftest 631/0).** Vijf commits sinds bd1eecd. *Item 3 (slider-live wattlist)* — c9e9b45 (server stempelt `pctLo`/`pctHi` per blok in `renderVariant_`; client `wattFromPct_`/`blokLijstSegs_`; de twee slider-call-sites geven `fromSegs=true`) + 1b98f0b (cache-key `trainlib_v1_`→`v2_`; de stale `getTrainingLibraryCached_`-blob serveerde de pre-shape library — selftest miste 't want die roept de verse builder). *Archetype-diversiteit* (3 commits): **rotatie-lite 015bfe9 (baseline 485)** — `goalWorkout_`-archetype-keuze nu per-intent recency-VENSTER (`intentRec`-filter: ongebruikte vormen eerst → volledige rotatie vóór herhaling; stalest-first bij uitputting; `archetypeVoorkeuren` = zachte bias); `lastId` weg; +`testGoalWorkoutRotatie_`; id-avoid-test herschreven naar same-intent. **diepe seed 091f7c9 (baseline 488)** — `gatherWeekplanEntries_(horizonWeeks, baseMonday)` (Algorithm.gs, `RECENCY_HORIZON_WEEKS=8`) seedt uit de laatste 8 weekplannen i.p.v. één; `recencyFromWeekplan_` ongewijzigd; `wpRaw0`-guard weg (blokkeerde anders bij lege huidige week); +`testGatherWeekplanEntries_`. **12 forms 9326fa7 (baseline 631)** — ARCHETYPES 9→21 (VO2: vo2_4x5/vo2_40_20/vo2_30_15_sets/vo2_sandwich; drempel: threshold_2x20/threshold_4x10/threshold_overunder_long/threshold_pyramid; sweetspot: sweetspot_3x15/sweetspot_4x12/sweetspot_overunder/sweetspot_long_climb); sandwich-structuurtype geactiveerd; `testArchetypeLib_` itereert + dekt alle 21 auto. **`generateProposal` = ÉÉN week**, geseed uit de huidige-week-snapshot + nu 7 voorgaande weken. **VERIFICATIE OPEN:** /dev — genereer + 2-3× regenereer → andere kwaliteitsvormen per keer; sandwich / 40-20 / over-unders renderen correct (watt-lijst + zoneBar).
- **AUTONOME TEST-GATE LIVE (d16df36).** `clasp run-function runSelfTest` draait remote (GCP Apps Script API aangezet op project `coachdaan`; executionApi `MYSELF` + project-scoped creds + `api-exec`-deployment). Gate = `node test-gate.mjs` (= `clasp push -f` + run-function; non-zero exit bij `failed>0` OF `passed<BASELINE 668`). CLAUDE.md herschreven naar de autonome CC-loop. Handmatige selftest-loop + `_Diag`-plak-loop vóór de selftest VERVALLEN; CC self-verifieert vóór rapport. Setup-prep `098fc8c`, gate+CLAUDE.md `d16df36`. Leerpunt: `clasp enable-api` = advanced services, NIET de project-API → Apps Script API via GCP-console enablen.
- **WEEK-ALLOCATOR FASE 1b CODE-COMPLEET + SIMULATIE-GEVERIFIEERD (selftest 479/0).** Week-niveau kwaliteits-allocator als pre-pass vóór de greedy day-loop in `assignWorkouts`. De engine was puur greedy per-dag (geen week-vooruitblik/quotum); `allocateQualityWeek_` kiest nu week-breed wélke dagen quality dragen + lost intent/archetype + endurance-fill op; de day-loop consumeert het plan via één nieuwe tak (`allocActive && quotaPlan[dagIdx]`) ná de pre-branches, vóór de bestaande pendel/weekend/vrij-takken (die blijven als !allocActive/voorbije-dag-fallback). Commits: C1 36d6809 (duur-haalbare intent-selectie `intentHaalbaar_` + optionele coverage-bias `COVERAGE_BOOST_`/`INTENT_PRIMARY_BUCKET_` in `goalPickIntent_`/`goalWorkout_`; `keyIntensity` geeft `dekking` door), C1b 0bd505c (COVERAGE_BOOST_ 0.30→0.10 — modulerend i.p.v. overrulend; 0.30 overrulede beslissende ftp-gewichten), C2 870107a (PROFILES-velden `kwaliteitPerWeek`/`spreiding`/`langeRitPerWeek` op klim+ftp), C3 073aeaf (pure `allocateQualityWeek_` + helpers `isHardType_`/`primaryBucketOfType_`, dormant; +16 `testAllocateQualityWeek_`), C4 b8ff7da (wiring + pendel-aware post-passes + DEMOTE_MAP `combo_long_with_efforts→long_z2`). **Architectuur:** `allocActive` = Base/Build/Peak (NIET Recovery/Test/event-Recovery); taper-dagen uitgesloten (`dayTapers`). **Pendel = constraint i.p.v. categorie:** heen altijd Z2, terug = volwaardige quality-kandidaat (gelijke voet, lichte voorkeur bij gelijke opties) op `pendelDuurMin` via de bestaande asymmetrische multi-session-split (laatste sessie draagt type+archetypeId); niet-gekozen pendel → `pendel_z2` (pure Z2 — auto-terug-intervallen vervallen in Base/Build/Peak). **Lange rit beschermd** (langste eligible niet-pendel dag, `langeRitPerWeek:1`); klim Build/Peak → `combo_long_with_efforts` (lange rit + efforts = verbruikt 1 slot); Base + ftp → puur `long_z2`. **Profiel-laag draagt beleid:** `kwaliteitPerWeek {Base:2,Build:3,Peak:2}`, `spreiding {midweekMinGap:1, weekendBlok, effortsInLangeRit}`, `langeRitPerWeek`. Klim weekendBlok/efforts=true; ftp=false → ftp blijft drempel/sweetspot-zwaar. **Spreiding:** geen aaneengesloten harde dagen (anchors incl. `recentHardDate`), behalve Sa-Zo-paar bij weekendBlok (deprioritized). Debt pre-claimt 1 slot (`debtPreferredType_`). **Coverage modulerend + FORWARD-planned:** `cov` geseed met rolling-7d + combo(high) + debt, doorgegeven aan `goalWorkout_` per quality-dag (boost 0.10 tipt nauwe keuzes naar ongedekte bucket, respecteert beslissende gewichten); drempel/sweetspot→high, vo2→anaerobic, quality raakt nooit low. **(a) duur-bewuste intent + (b) dekking-bewustheid — beide archetype-fase-1-openpunten OPGELOST.** **EMPIRISCH OPEN (Build/Peak-geblokkeerd — taper):** /dev-verificatie van de week-plaatsing (quality-spreiding, pendel-terug-archetype, combo-lange-rit, `reden`-weergave) alleen in een NIET-taper Build/Peak-week zichtbaar (taper-week → alle dagen gegate → allocator inert); demo-hook (à la rdydemo) of wachten op Build/Peak. **FOLLOW-UPS:** (3) *done-quality quotum-reductie* — **KLAAR (a21b5dd)**: optionele `weekDays`-param door `assignWorkouts`→`allocateQualityWeek_`; `doneHard` telt nu uit `weekDays || days` (fallback houdt de 4 bestaande 11-arg testcalls byte-identiek), `generateProposal` geeft de volle 7-dagen `days` mee → `remaining = quota − done-quality-deze-week` leeft. +1 `testAllocateQualityWeek_` (base 3 / reduced 2; pendel-only fixture, want klim `effortsInLangeRit` eet anders een quality-slot). Live taper-geblokkeerd; gate-geverifieerd. (4) *weekend-aware avoid-hard* (optioneel/laag-prio) — vangnet (gap 1, niet weekend-aware) degradeert een geforceerd Sa-Zo-paar → weekendBlok stuurt allocatie maar niet eind-output (matcht B's conservatieve intentie). **Keten:** 456 → +5 `testGoalWorkout_` (C1) = 461 → C1b assert-herziening +1 = 462 → +16 `testAllocateQualityWeek_` (C3) = **478/0** (C4=0) → +1 testAllocateQualityWeek_ (done-quality threading, a21b5dd) = **479/0**.
- **ARCHETYPE-GENERATOR FASE 1 CODE-COMPLEET + SIMULATIE-GEVERIFIEERD (selftest 456/0).** Doel-agnostische archetype-bibliotheek + verwisselbare profiel-laag + generieke selector, ter vervanging van de monotone vaste-structuur-generatie. Commits: fundering 1bc4c5c (schema + expandArchetype_ + 3 fixtures), bibliotheek 89f62df (register ARCHETYPES, 9 records drempel/sweetspot/vo2), werk-range 0597b56 (optionele onPctLo/onPctHi op int-werk + expander + fallback), profiel+selector c5366be (PROFILES klim/ftp + goalWorkout_ + profileForDoel_), inplug 45dfc1d (plumbing: buildWorkout-tak archetypeId→expandArchetype_, archetypeId door generateProposal/Proposal.gs/weekplan-keten, recencyFromWeekplan_) + 955c134 (activatie: keyIntensity Build/Peak-tak → goalWorkout_(profileForDoel_(settings.doel)) via out-param; d.archetypeId gezet + gewist bij avoid-consecutive-hard-downgrade én wellness-demote; climbTypeWorkout_ blijft FALLBACK bij null), sim d0b6bc6 (Build/Peak-weeksimulatie als permanente selftest testGoalInplugWeekSim_). **Architectuur:** archetype = data-record {id, structuurtype (METADATA — expander vertakt op core[].kind, niet structuurtype), effectTags (⊆ Coach-intent-vocab COACH_TYPE_INTENT_ excl 'vrij'), zone (int 1..6 display), duurRange, warmup/core/cooldown/fill}; core-grammatica = pool-{kind:'steady'|'int'} (over-unders/pyramids = steady/int-sequenties, geen expander-uitbreiding nodig). expandArchetype_(rec,ctx{ftp,lthr,doelMin,mesoFactor,faseOffset}) → output-contract = byte-compat workout-object + blokken[{minuten,zone(bucket-STRING),pctLo,pctHi}] (numerieke pct → push robuust + Item 3 ontgrendeld) + structuur in de bestaande grammatica (push-parsebaar via dslBlockFromRow_/dslPowerRange_). Profiel = {intentGewichten over drempel/sweetspot/vo2, faseModulatie (additief, Base sweetspot/drempel-zwaar / Peak vo2-zwaar), archetypeVoorkeuren, projectieKey→GOAL_PROFILES_}. goalWorkout_ DETERMINISTISCH: intent (gewogen × fase, recency-mijdend) → filter effectTag+duurRange⊇tijd → archetype (recency-gewogen) → {type via COACH_INTENT_ENGINE_TYPE_ (drempel→threshold/sweetspot→sweet_spot/vo2→vo2max — alle drie in COACH_TYPE_INTENT_/workoutZones/DEMOTE_MAP/INTENT_BADGE → inplug zonder map-entries), archetypeId}. **Gedrag-shift (Build/Peak):** goalWorkout_ is nu PRIMAIR; climbTypeWorkout_'s klimType-specificiteit BEWUST losgelaten t.g.v. profiel-diversiteit; de dekking-bewuste doel-tak + trip worden geschaduwd TENZIJ goalWorkout_ null (>135min → trip/long_z2; sub-duurRange → doel-tak/climb-fallback). Base ongewijzigd. **EMPIRISCH OPEN (fase-afhankelijk, alleen Build/Peak):** DocProps recency-seed + write→dashboard-keten via live generateProposal + de UI-weergave van archetype-workouts (keuze-logica zelf is via de simulatie gedekt — keyIntensity bleek volledig puur).
- **NIVEAU FASE-2 COMPLEET (269/0).** §c Rijdersprofiel (3615d37/ee3d91f): lazy getPowerCurve (intervals.icu /athlete/{id}/power-curves?type=Ride; rauwe-respons-cache powercurve_raw_<window>_<yyyyMMdd>, re-normaliseer-op-read), power-duration-curve log-x + markers 5s/1m/5m/20m/60m + type-schaal + eFTP-subregel (§a, idx14 rolling-FTP, API-vrij). Rider-type her-verankerd op intervals.icu's 4-anker-W/kg-methode (371d35a; was 5s/20min-ratio → 'Sprinter', nu All-rounder, gevalideerd op Daans cijfers). Cache-fix 46f3e1e: rauw cachen i.p.v. genormaliseerd → logica-wijzigingen slaan direct door. §c venster-switch (6e3fb79): getPowerCurve(window) via curves=<id> (bevestigd 90d/1y; start/end/oldest/newest GENEGEERD), segmented 90d/1y. §d DoelProjectie (1077c4d): swap-able GOAL_PROFILES_/activeGoalProfile_ (girona: klim 4,0 W/kg · duur CTL 65 · lange-rit 4u); doel-gap-rijen + uren→potentieel-what-if met TWEE visueel-onderscheiden lagen — SOLIDE CTL-ramp (ctlPlateauFromVolume_/ctlApproachWeeks_, tau42d) + klaar-marker, SPECULATIEVE gestreepte FTP-schatting-band (ftpBandFromProjection_, vloer=huidige FTP, altijd bereik) + aannames. Pure calcs server-getest; slider = client-mirror. **Vorm + v1-snapshot/-progressie ONAANGEROERD.** Visueel geverifieerd (Daan, /dev).
- **DONKERE SKIN IS LIVE.** 4 tabs. **Schema-tab compleet** (plan-kaart 1a, WeekLoad 3b, dag-detail skin 3a, web-RPE + skip-dispositie 3c, §1d zone-naming naar 't 6-zone-ontwerp, deel-2 pass-3a carry-overs, pass-3b AvailabilityEditor per-dag (overlay-sheet), full-width layout, **globale app-header** (FTP COACH + ISO-weeknr + DK-avatar), **DEZE WEEK · GEPLAND VS GEDAAN**, **type-schaal-conformance**, dag-detail-viz + power-per-blok, design-conformance batch 1+2, knip(a)+(b)). **Vorm-tab compleet**: ReadinessCard (1b) + check-in (2) + LevelCard/MetricRow/chart-tokens (3-d1) + niveau-grafiek (3-d2) + Conditie Balans-meter (3-d3) + swipe-deck (ReadinessCard⇄LevelCard) + LevelCard tier-chip/blok-%/reconciled-delta (3-d4). Alle 4 tabs zijn nu gebouwd (Schema/Vorm/Trainingen/Niveau); resterend = finishing touches + functioneel-restant (zie Volgende stap).
- **VISUEEL GEVERIFIEERD op /dev** (Daan, mobiel incognito): 1b + check-in + Schema 3a/3b/3c, AvailabilityEditor, app-header, DEZE WEEK · GEPLAND VS GEDAAN, type-schaal, **dag-detail-viz + power-per-blok**, conformance-fixes. **Schema + Vorm + Trainingen zijn qua DESIGN compleet.**
- **INSTELLINGEN-DRAWER LIVE** (avatar→drawer; 5 secties + Events-CRUD met bewerkbare details; één Opslaan). **TAPER-MODEL Deel 1+2 live** (event-nabijheid vanaf vandaag; A-taper ≤7d volle week + B-mini-taper ≤3d per-dag-gating; macro blijft van 't A/trip-event). Zie KLAAR.
- **TRAININGEN-TAB LIVE** (bibliotheek per categorie → categorie → workout-detail; status-deck + DurationSlider + "Inplannen"). **DAY-OVERRIDE LIVE** ("Andere training kiezen" → WorkoutPicker; bibliotheek- of vrije/groepsrit-override; respect-op-replan). **COACH-FEEDBACK op dag-niveau LIVE**: voltooid (op-plan/afgeweken/anders met gepland-vs-gedaan-lezing + reële zone-bars + %-balk + COACH-callout) + gemist (skip-reden + COACH·IMPACT) met **doel-bewust, gedifferentieerd narratief** (event-demand + patroon-escalatie). Zie KLAAR.
- **ADAPTATIE-EXECUTIE LIVE** (be33ef7): "Plan deze aanpassing"-knop in DoneDetail(afgeweken)/GemistDetail → `saveDayOverride` van een ingekorte make-up op de eerstvolgende plannbare dag (idempotent, omkeerbaar). **RIT-DETAIL v1 LIVE** (60254ca): tik "Bekijk ritdetails" → overlay-sheet met intervals.icu-activiteitstatistieken (kop · tijd-in-zone · NP/IF/TSS · metric-grid · zone-gestripte intervallen), lazy + gecachet. **BOTTOM-NAV-ICONEN LIVE** (02e7a31): line-iconen vervangen de emoji's. Zie KLAAR.
- **ZoneCompare LIVE** (f5a214c): DoneDetail-only, één rij per zone (faded gepland + massief gedaan op één minuten-schaal, "niet gepland"/"niet gereden"-tags); GemistDetail bar-loos (voltooid-only). Zie KLAAR.
- **Rit-detail 6-tegel-grid + W/kg LIVE** (f5a214c): 6 tegels (Gem W · W/kg · Gem HR+max-sub · Hoogtewinst · Cadans · Arbeid kJ); VI/Max-HR-tegel/kcal weg; server-veld gewicht + `rdWkg_`. Zie KLAAR.
- **`--zcompare-*` (7) geland** (c1f61f8, two-file mirror); design-import gefold (§1bis/§1ter, INTERACTIONS, frames 08-11).
- **COACH-CLASSIFICATIE-FIX LIVE** (Stage 1 eb8e718 + Stage 2 0497611): planned-intent zone-afgeleid i.p.v. type-label → fantoom-"Duur→Drempel"-swap weg; reading-Type relabelt; zelfde-intent-onder-volume-narratief; patroon-teller mee-gefixt. Zie KLAAR.
- **NIVEAU-TAB v1 LIVE** (a104a60 fold / e0eb120 server / ff26b04 client): de **4e gebouwde tab** (was lege placeholder). Vermogen-snapshot (FTP-hero · W/kg-pill · tier-chip · 6-rung ladder) + progressie-kaart (metric-switch Niveau/W-kg/Fitheid · venster 1M/6M/12M/Alles · periode-delta · gestippelde CTL-overlay) + minimale "Fase 2"-kaart (rijdersprofiel/doel-projectie, geen backing). **Vorm ONAANGEROERD.** Zie KLAAR.
- **COACH COPY-NUANCE** (dbaec81: drempel-substitutie = klim-specifiek credit), **DAGSTRIP-DOT OVERRIDE-KLEURING** (db4e62b, visueel geverifieerd) + **DISPOSITIE TOGGLE-TO-CLEAR** (6641b10) live. Zie KLAAR.
- **runSelfTest — Stand: 668 passed / 0 failed.** Keten: 239 (echte STAP-2-basis; de '240' was schatting) → +13 testPowerCurve_ (§c, 3615d37) = 252 → rider-type-reanchor net 0 (371d35a) → +1 live-fixture (cache-fix, 46f3e1e) = 253 → +16 testGoalProjection_ (§d, 1077c4d) = 269 → +25 archetype-fundering (1bc4c5c) → +99 testArchetypeLib_ (89f62df) → +7 werk-range (0597b56) → +17 testGoalWorkout_ (c5366be) → +19 inplug testInplug_/testKeyIntensityInplug_ (45dfc1d/955c134) → +19 testGoalInplugWeekSim_ (d0b6bc6) = 456 → week-allocator-keten → **478/0** → +1 testAllocateQualityWeek_ (done-quality threading, a21b5dd) = **479/0** (zie de WEEK-ALLOCATOR-bullet hierboven) → Item 3 +1 (`testTrainingLibrary_` pctLo/pctHi, c9e9b45) = 480 → rotatie-lite +5 (`testGoalWorkoutRotatie_`, 015bfe9) = 485 → diepe seed +3 (`testGatherWeekplanEntries_`, 091f7c9) = 488 → 12 archetypes +143 (`testArchetypeLib_` × 12 records, 9326fa7) = **631/0** → Pass 1 +12 (`testVolumeModulatie_` +10, Base-reconcile +2; term de31489 / wiring 6c1c64d) = **643/0** → cov-gate +6 + vo2max/conditie-profielen +10 (`testCovGateBase_` / `testProfielenVo2maxConditie_` +8 + mapping-reconcile +2, 4b1ad35) = **659/0** → knip-a freeze-first +9 (`testSnapshotDayAction_`, e9ba7b7) = **668/0**. Draait remote via `node test-gate.mjs`.
- **LAADTIJD — structurele optimalisatie KLAAR** (cdc2f91/851a51e/44fac0c/5c5805a): open-flow ontdaan van redundante I/O. De oude "11,4s · GÉÉN API"-claim was FOUT op twee punten: (a) de open-flow deed 3 live intervals.icu-calls; (b) 11,4s was een belaste-server-uitschieter (cold raakte deze sessie 9038ms onder GAS-load). Na optimalisatie: **1 API-call** (alleen `computeZoneDebt_`'s `getActivities(14)` — zone-tijden staan niet in de tab). Schone steady-state-meting in ms staat open (GAS-timing was de hele middag te jittery — canary `getTrainingLibrary_` §3 swingde 385↔1139ms; verificatie was structureel). Kalme post-prompt-2-baseline = 3526ms warm; off-peak `diagLaadtijd2` bevestigt het cumulatieve totaal.
- **CADANS-REBRAND + VORM-A LIVE (STAP 1).** *Rebrand* (e4d380c; favicon-keten SVG→raw-crash→PNG-jsDelivr-crash-safe, eind daa32e9): CadansMark = inline SVG in `#appbar` (`.appbar-mark`; 3 stijgende `skewX(-12)`-balken, fill `var(--accent)`→`var(--accent-strong)`); #appbar-wordmark = dynamisch `state.coachName` (uppercase, default "COACH"); coachnaam-instelling **"Jouw coach"** (drawer-sectie bovenaan: tekstveld + chips) → `saveSettingsEditor`→`saveSettings` → `SETTINGS_FIELDS.COACH_NAAM` (rij 54) + DocProp `coach_naam` (raakt `API_KEY` niet) → `state.coachName`, in wordmark + coach-callout-overline; `setTitle('Cadans')`. Favicon = `favicon.png` (repo-root, uit pakket `cadans-icon-512.png`) via jsDelivr (`cdn.jsdelivr.net/gh/daanhhk/training@main/favicon.png`), `setFaviconUrl` in **TRY/CATCH** in `doGet` (crasht nooit). Merk "Cadans" = mark + tabtitel; wordmark = coachnaam. *Vorm Variant A* (248527d, pakket-volgend): deck-LevelCard (#vorm-level) W/kg-geleid — hero "{wkg} W/kg" (`state.ftp/gewicht`, 1 dec, null-guard), tier-chip via `NV_TIERS`/`nvTierIndex_` (W/kg-afgeleid; Beginner/Recreatief/Getraind/Gevorderd/Zeer goed/Elite; NIET `niveauTier_`), tier-voortgangsbalk = positie-in-band + "nog {x} tot {volgende}", W/kg-delta uit `state.niveauProgressie`; "x/50"+niveau-hero+blok-% weg. VormLevelSummary (#vorm-metrics) vervangt niveau-grafiek + MetricRow = FTP/Gewicht/Week-TSS + "Progressie →"→`switchTab('niveau')`; `drawNiveauChart`-mount+calls weg; deck blijft 2 slides; volgorde deck→summary→balans. ONTDUBBELING: W/kg+tier exclusief in deck-LevelCard. TIER consistent Vorm↔Niveau (beide `NV_TIERS`). Geen tokens; `--chart-grid` + `niveauTier_`/`testTier_` ongemoeid → **224/0 ongewijzigd** (Daan handmatig). Zie KLAAR.
- **STAP 2 — readiness→Schema-koppeling: CODE-COMPLEET** (selftest **240/0**, was 224). *Deel 1 server* (89221c1): pure `readinessAdjust_(planned, band, macroFase)` in Coach.gs (+ `readinessEaseNaam_`/`readinessRegel_`/`readinessRegelDone_`); `getDashboardState`-overlay zet `day.coach` (`kind:'readiness'`) op de vandaag-PLANNED dag bij demote; `saveDayOverride` clean-object draagt src-passthrough (`if(ov.src)`); `testReadinessAdjust_` (+16); `getReadinessScore_` ge-hoist naar één gedeelde `readinessState`. *Deel 2 client* (9035e95): `readinessBannerHtml_` (suggest: `--readiness-{band}`-dot + "Gereedheid N · woord" + "VERLICHT {from}→{to}" + knop "Verlicht vandaag"; committed: ✓-regel, geen knop) hergebruikt `.coach-*`-classes/-tokens (12/12 geserveerd, GÉÉN nieuwe); mount in `renderDagDetail` ná de weekdag-kop (vóór `var ov`), kind-gated; `planAdaptatie_` stuurt `src:ad.src` mee; commit = free-ride override (`src:'readiness'`) → committed-banner + FreeRideCard + "Terug naar voorstel" (revert via `clearDayOverride`). **Architectuur GELOCKT:** route B (read-side overlay + user-commit override, GÉÉN generatie-bake) · VERLICHTEN niet verschuiven · caution→`demoteType_` (één DEMOTE_MAP-stap) / rest→recovery · today-only · enkel-sessie-only (multi-sessie + per-sessie-ease = future) · onderdrukt in Taper/Recovery · leest 't HUIDIG opgeslagen type → componeert schoon met avoid-consecutive-hard + wellness-slot-pass (geen dubbel-ease). **OPEN — verify + close-out (volgende week, TAPER-geblokkeerd):** visuele /dev-verify staat nog (taper = Z2-only + ease onderdrukt → geen natuurlijke trigger; demo via TEMP `rdydemo`-hook op een toekomstige geplande dag, PARAM-EERST-dan-dag-tikken). TEMP `rdydemo`-hook (`// TEMP rdydemo`) staat NOG in Script.html (verruimd naar `trnPlannable_`, 325315c). Close-out = (1) /dev-verify suggest→commit→committed→revert, (2) rdydemo-hook verwijderen, (3) FTP-Coach-export §d + INTERACTIONS readiness-banner-secties folden, (4) HANDOFF-bump.

## clasp run-function — autonome CC-loop (KLAAR, d16df36)
run-function werkt remote; gate = `node test-gate.mjs`; CLAUDE.md herschreven (test-gate / mission-scoping / no-go's Deck-CSS·token-mirror·calc-consolidation / stop-and-ask / run-log / handmatige /dev-verificatie). Root-cause was de GCP Apps Script API (`script.googleapis.com`) die op project `coachdaan` ontbrak; executionApi `MYSELF` + project-scoped creds + `api-exec`-deployment waren al goed. Secrets (`creds.json`/`.clasprc.json`) gitignored + untracked (publieke repo).

## Visual system / aanpak
- **design-conventies: zie CLAUDE.md** ("Design — bron van waarheid": UI-build-loop, token-discipline, self-heal).
- Beslissing: **THEME-FIRST**. `design/tokens.css` (donker, 4-tabs) = styling-bron van waarheid; volledig gemirrord in `src/Tokens.html`.
- **TOKEN-MIRROR = REPO-WAARHEID (durable):** de runtime-token-DEFINITIES staan in EXACT twee bestanden — **`design/tokens.css`** (bron) ↔ **`src/Tokens.html`** (verbatim, dít wordt geserveerd). **GÉÉN root-`tokens.css`** (bestaat niet — negeer Claude Design's sandbox-`tokens.css`/`root tokens.css`; neem daar alléén token-definities uit over). Bij een Claude-Design-import: nieuwe tokens VERBATIM in béíde mirror-bestanden. Geverifieerd: `--align-*` / `--reading-*` / `--coach-*` / `--ride-*` / `--tiz-*` / `--interval-*` / `--skeleton-*` / `--state-*` / `--tabbar-*` staan in `design/tokens.css` + `src/Tokens.html`; `src/Script.html`/`src/Styles.html` **gebruiken** ze (`var(--…)`) maar definiëren niet. De `_import-design*/`-mappen zijn scratch (ongetrackt na fold).
- Het ONTWERP is leidend; bestaande functies bouwen we erin, en vullen aan waar nodig.
- **`design/FTP-Coach-export.md` = de feitelijke per-tab layout-bron** (componenten + states + tokens per scherm). `design/DESIGN.md` = visuele taal (geen per-kaart-layout). **`design/INTERACTIONS.md` = interactie-contract** (per-element READ/WRITE + 💻client/🌐server-gedrag; leidend voor UI-gedrag, zie CLAUDE.md).

## Readiness-formule (bouwklaar — 1b)
- Output 0–100; banden **≥62 ready / 48–61 caution / <48 rest** (uit `tokens.css`).
- Gewogen gemiddelde van genormaliseerde factoren (0–100). **Objectief-geleid (door Daan gekozen).**
- **GELOCKT deze sessie — objectieve/pré-check-in weging (= wat 1b bouwt; check-in bestaat nog niet):**
  - Vorm-trend **0,30** · Belasting (ramp/ATL vs CTL) **0,30** · HRV (vs baseline) **0,25** · Slaap (uit intervals) **0,15**. Som 1,00.
  - Vervangt de eerdere pré-check-in-getallen (30/22/20/13 rescaled). Schuifbaar: zet als named constant bovenaan de readiness-fn.
  - **3 presets** `objectief / gebalanceerd / subjectief` (objectief = default; de andere twee + benen/stress = toekomst, post-check-in).
- **Vol model (post-check-in, TOEKOMST):** voegt Benen + Stress in (subjectief, uit ochtend-check-in) en herberekent; gebalanceerd/subjectief schuiven richting slaap-zwaar (Garmin-achtig). Check-in-capture = future write-side (gebruikt de overlay-primitive uit Fase 0b).
- Ontbrekende factor → wegvallen + herschalen (geen harde nul). **HRV + slaap bevestigd aanwezig** in deze gebruiker's intervals.icu.
- "Waarom dit cijfer": per factor 0–100 + status-dot. **LIVE-drempel (`getReadinessScore_`): good ≥67 / warn 34–66 / muted <34.** (De pré-build ≥70/45–69/<45 is niet geïmplementeerd.)

## Bouw-roadmap (volgorde)
Verticale plakken: UI + Apps Script-handler + Sheet-range per feature. Elke fase = bruikbaar increment.
- **Fase 0 Fundament — DONE.** skin-flip (tokens geserveerd + alias-bridge + IBM Plex + ring→tokens), 4-tab-shell (Trainingen/Niveau lege containers), herbruikbare overlay (sheet/drawer).
- **Fase 1 Status-deck & plan herstructurering — 1a DONE / 1b DONE.**
  - **1a DONE:** Schema-plan-kaart (PeriodTimeline, countdown verhuisd hierheen); deck VERWIJDERD van Schema (design: Schema heeft geen deck).
  - **1b DONE:** readiness-kaart (`ReadinessCard`, export §2a) op Vorm-top; `getReadinessScore_` (objectief preset) → `state.readiness`; vervangt de oude deck-mount.
- **Fase 2 — ochtend-check-in DONE; rest van Schema open.** DONE: check-in-sheet + readiness-bijstelling (±2-model). Open: WeekLoad + stale (F.3), dag-detail-varianten, WorkoutPicker/override, RPE, rust/niet-beschikbaar + toch-trainen (F.2), multi-sessie, edge states.
- **Fase 3 Rest van Vorm:** level-kaart + niveau-grafiek + metrics + conditie-balans; **+ chart/legenda tokeniseren** (off-palette leftovers, zie contracten).
- **Fase 4 Trainingen — DONE.** bibliotheek drill-down (cats/category/workout) + DurationSlider + "Inplannen"; **day-override** (WorkoutPicker) + **coach-feedback op dag-niveau** (voltooid/gemist, doel-bewust narratief) als afgeronde stromen erbovenop. Resterend headline = **adaptatie-EXECUTIE** (auto-replan van de coach-suggestie).
- **Fase 5 Instellingen-drawer** (CRUD; gebruikt overlay drawer-variant). Naar voren te halen indien nodig.
- **Fase 6 Garmin-push** (leaf; incl. teal-knop tokeniseren).
- **Fase 7 Niveau** afmaken + QA-pass.

## Beslissingen & invarianten
- **Schema heeft GEEN status-deck** (design/export §1: trainingsgericht). Status-deck alleen op Vorm/Trainingen/Niveau.
- **INGETROKKEN:** ".status-card/.status-wrap niet aanraken" — ontwerp herwerkt die kaart bewust.
- **Blijven:** `google.script.run` only (geen losse fetch); tss zone-gewogen via `tssFromZoneMinutes_`; v2b-C leidt nooit `'recovery'` af; `design/tokens.css` = styling-bron; readiness = read-side (server rekent, client rendert).
- **[#3]-CORRECTIE:** client leest server-`mode` NIET. countdown = `vorm.event.dagenTot`; "Onderhoudt" = `statusVoor` (vorm/ramp). Mode read-/write-side client = toekomst.
- **Zone-model = 6 zones** (`tokens.css`: 1 Herstel · 2 Duur · 3 Tempo · 4 Drempel · 5 VO2max · 6 Anaëroob = Coggan Z1-Z6, geen Z7). Engine = **5 display-buckets** (`pctZoneBucket_`:1810 — rust<56 / z2≤75 / tempo≤90 / drempel≤105 / anaeroob>105; alles >105% gemerged). TSS/load = **3 buckets** (`tssFromZoneMinutes_`). De >105%-top = label "VO2max" / `--zone-5`; `--zone-6` ongebruikt (token blijft).
- **Schema-§1d-badge = workout-INTENT** (`intentBadge_`/`INTENT_BADGE`), GÉÉN zone-naam — de legenda toont de zones al. "zone-badge (`--zone-*`)" in export = de KLEUR-koppeling, niet de tekst. Categorie-namen (Sweet Spot/Klim/Duurvermogen/FTP-drempel) = Trainingen-bibliotheek (`CategoryCard`, INTERACTIONS.md:220) + Doel-grid (:258), niet de Schema-badge.

## Hergebruik vs nieuw
- **Hergebruiken:** dagstrip, `assignWorkouts`/`voorgesteldType`, availability F.1a, `getWellnessSignal`/`getFormScore_` (readiness-factoren), niveau-block (→ Fase 3 Vorm-body), intervals.icu-sync, `ringSvg` (readiness-ring), overlay-primitive (check-in-sheet + instellingen-drawer).
- **Echt nieuw:** readiness-score + kaart (1b), check-in-sheet + opslag, WorkoutPicker + override, RPE-UI + opslag, Trainingen-bibliotheek, Instellingen-drawer, Garmin-push, edge states, stale-banner (F.3), toch-trainen (F.2).

## Live stand (geverifieerd, na Fase 0 + 1a)
- HtmlService web-app, **DONKERE skin live**. 4 tabs: `#tab-schema` `#tab-vorm` `#tab-trainingen` `#tab-niveau` + `#bottomnav` (generiek gewired). **Globale app-header `#appbar`** (FTP COACH + ISO-weeknr + DK-avatar) bovenaan, buiten de tab-secties (vervangt de per-tab `.hdr`). Trainingen/Niveau = inline-styled placeholders.
- Schema-top = **plan-kaart** (`planCardHtml(state.plan)`); deck weg van Schema.
- Vorm-top = **ReadinessCard** (1b) in swipe-deck met LevelCard (de oude `statusGraphicHtml`-deck is vervangen).
- Overlay-primitive **in gebruik**: ochtend-check-in-sheet + AvailabilityEditor (`window.openDrawer/closeDrawer`).
- Off-palette leftovers: **Vorm/niveau chart-hex getokeniseerd in Fase 3 deel 1** (gridlines waren `#eef2f7`, niet `#e2e8f0`; PMC-lijnen + formZone-banden → tokens via `cssColor_`). Resterend: teal "Push naar Garmin"-knop → **Fase 6**.
- Alles via `google.script.run`.

## Architectuur
Per-user Sheet, geen centrale backend, Apps Script. Elke server-actie = handler + Sheet-range.

## Bronnen (commit-gepinde raw-URL, nooit blob)
`design/tokens.css` (styling-bron, gemirrord in `src/Tokens.html`), `design/DESIGN.md` (taal), `design/FTP-Coach-export.md` (per-tab layout-bron), `design/screenshots/` (01-schema..07-plan-card), `design/F-beschikbaarheid.md`, **`design/INTERACTIONS.md` (interactie-contract, per-element gedrag).**

**Test-gate:** `src/SelfTest.gs` (COMMITTED, permanent — geen wegwerp-`_Diag`). `runSelfTest()` draait de pure-engine asserts en logt `SELFTEST: X passed, Y failed` + returnt `{passed, failed, failures[]}`. Draait via `node test-gate.mjs` / `clasp run-function runSelfTest` (self-enforced gate, geen commit tenzij groen). **Stand: 668 passed / 0 failed.** Cases: `testTrainingLibrary_` (per-categorie integriteit, schaalt met variant-count), `testDayOverride_`, `testCoach_` (IF-normalisatie 77→0,77, zone-intent, doel-bewuste/patroon-tiers), `testCoachAdaptatie_` (+11: payload geldig + variantId in library + null-cases). **Keten (historisch):** 190 → +12 `testRideDetail_` → +4 `rdWkg_` → +6 `testCoach_` (Stage 1) = 212; +3 coach copy-nuance = 215 → +9 → 224 → +16 → 240 → … → **668/0** (volledige keten in de STAND-bullet).

## Nieuwe contracten (Fase 0 + 1a — geverifieerd)
**Skin (0a):** `src/Tokens.html` = `<style>` + verbatim `design/tokens.css`. `src/Alias.html` = old→new `:root`-bridge + `html,body{font-family:var(--font-sans)}`. Include-volgorde Index `<head>`: fonts(IBM Plex) → Tokens → Styles → **Alias (overschrijft Styles' legacy lichte :root)**. Base-selector = `html, body` (Styles.html:17). `ringSvg` (Script.html:309) strokes via inline `style`: track `var(--ring-track)`, progress `var(--accent)`.
**4-tab-shell (0b):** hardcoded `<section id="tab-{schema|vorm|trainingen|niveau}">`. `#bottomnav` `<button data-tab="..">` (emoji-`<span>`+bare-text), generiek `querySelectorAll('#bottomnav button')→switchTab`; literal `['schema','vorm','trainingen','niveau']`; niet-actief inline `display:none`, init `switchTab('schema')`. Nieuwe secties nog zonder `class="tab"` (onschadelijk).
**Overlay (0b):** `src/Drawer.html` CSS (head, ná Alias). Markup vóór Script-include: `#overlay-scrim[data-overlay-close]` + `#overlay-panel[data-variant=sheet|drawer]` (off-screen transform, `.is-open`) + `.overlay-handle` + `#overlay-content`. `window.openDrawer({variant,html})`/`closeDrawer()`: `body.overlay-open` scroll-lock, aria-hidden, focus+restore; Esc + gedelegeerde `[data-overlay-close]`. z-index scrim 1000/panel 1001. **Basis voor ochtend-check-in-sheet (F2) + instellingen-drawer (F5).** Console-test = `userHtmlFrame`-context.
**Deck (pre-1b, nu alléén Vorm):** `statusGraphicHtml(sfx)` (Script.html:309) → `.status-wrap#swrap-{sfx}` (scroll-snap+`syncDots`). card1 = `.status-left` (`ringSvg`+`.fase-pill`) + `.status-right` (`.status-word`/`.status-verdict`); card2 `.niveau-block`. `renderVormStatus`→`#vorm-status`. `statusVoor(v)` (Script.html:287) = FUNCTIE→labelstring (leest `v.huidig.vormZone`/`ramp`, `v.rampBuildMin`), geen object.
**Readiness-data (server, read-side — 1b-input):** `getFormScore_`→`{date,ctl,atl,form,ramp,label}`. `state.vorm.huidig`=`{vorm,vormZone,ctl,atl,ramp}` (WebApp.gs:471) + `vorm.macroFase/ftp/rampBuildMin`. `getWellnessSignal` (Algorithm.gs:888)→`{hrvBaseline(28d),hrvRecent(3d),sleepLastNight,sleepAvg3,hrvDeficit%}`. Wellness-tab (Algorithm.gs:900): A=Datum B=RHR C=HRV D=Slaap; baselines computed; cadans `getWellness(30)` bij `syncAll`. **Check-in (ochtend, benen/stress/slaap) = AFWEZIG** (future); `rpeAvondCheck` (Telegram) = avond-RPE, los.
**Plan-kaart (1a, live):** `buildPlanModel_(macro, settings)` (Doel.gs) → `state.plan` (WebApp.gs, na `mode`) = `{modeLabel, eventName, wekenTot, dagenTot, currentPhaseKey, currentPhaseLabel, phases:[{key,label,state}], events:[{naam,prio,type,dagenTot}], volume:{label,value}|null}`. Hergebruikt `bepaalFaseVoorDatum_(weekStart)`→`{fase∈Base/Build/Peak/Taper/Recovery, wekenTotEvent, eventDriven, eventName, eventDate}`, `computeMacroPhase(start,today)`→`{week,fase∈Base/Build/Peak/Test,isTestWeek}`, `getAllEvents_`, `getVolumeTargets()[fase]=[min,max]u/wk`. NL-labels Basis/Build/Peak/Taper. `planCardHtml(plan)` (Script.html, `<details>`, default dicht); CSS in Styles.html. Edge: Test→Taper-bucket(idx3), Recovery→alle-4-past (geen current-marker) — herzien als 't leeg oogt.

## 1b — bouwspec (DONE — contracten in KLAAR hieronder)
Vorm-top `ReadinessCard` (export §2a), vervangt `renderVormStatus('vorm')`→`#vorm-status`-mount.
- **Server** `getReadinessScore_` (combineert `getWellnessSignal`+`getFormScore_`) → `{score 0–100, band(ready/caution/rest), factors:[{key,label,value0-100,status:good/warn/muted}], chips:[{label,tone}]}`. Gewichten als named constant (presets objectief/gebalanceerd/subjectief; default objectief = 0,30/0,30/0,25/0,15). Normaliseer per factor (vorm-trend uit form/TSB, belasting uit ramp/ATL-CTL, HRV uit hrvDeficit/baseline, slaap uit sleepAvg3/need). Expose via getDashboardState (bv. `state.readiness`).
- **Client** `readinessCardHtml`: overline "Status · vandaag"; `ringSvg` gekleurd naar band (`--readiness-ready/caution/rest`, track `--readiness-ring-track`, center `--font-num`); verdict + chips ("Vorm +n" `--fresh/--fresh-soft`, "HRV n" `--text-muted`); "Waarom dit cijfer?"-expander → factor-lijst met status-dots. Onder: gestippelde "+ Ochtend-check-in invullen"-prompt — capture is TOEKOMST. niveau-block valt uit Vorm-top (herbouw = Fase 3).
- STAP 0-recon: `renderVormStatus`/`#vorm-status`-mount, `getWellnessSignal`/`getFormScore_`-shapes, getDashboardState-assembly, `ringSvg`-kleurparam.

## Open punten / tech-debt (post Fase 3c)
- **Schema deel 2 — besluiten:** Garmin = **per-dag sync** (5 states, pass 6). 'Gemist' + Overridden + FreeRide = **allebei** (skip-dispositie blijft; Overridden/FreeRide read-side erbij, pass 4). Connect/Sync/Empty → **alléén 'laatst gesynct'-indicator** (pass 2); Connect+Empty **overgeslagen** (één altijd-verbonden user; revisit bij Scope B). AvailabilityEditor per-dag (pass 3b — KLAAR); WorkoutPicker + Toch-trainen (pass 5); weekplan-regen-refresh + stale-banner (pass 2).
- **Schema deel 2 — pass-roadmap:** pass 1 dag-detail-display + glyph + check-in-migratie (**KLAAR**) → pass 2 WeekLoad regen-refresh + stale-banner + laatst-gesynct + zone-badge-fix (**KLAAR, visueel geverifieerd**) → **pass 3a carry-over-bundle + zone-naming (KLAAR, geverifieerd)** → **pass 3b AvailabilityEditor per-dag (KLAAR, geverifieerd)** → pass 4 Overridden/FreeRide read-side → pass 5 Recovery "Toch trainen" + WorkoutPicker → pass 6 per-dag GarminSync (vervangt week-"Push").
- **Pass-2 carry-over — (a)(b)(c)(d) OPGELOST** (pass 3a + zone-naming, zie KLAAR): (a) **zone-badge** → `intentBadge_(type)` (445f5b3) + zone-naming/VO2max-fix (2b1c893/36431fc); §1d-badge-definitie = workout-INTENT (zie invariant). (b) **laatst-gesynct** → `regenerateWeb` stampt nu `last_sync` via expliciete `syncAll` (f0087ad, geverifieerd 13:24→14:34). (c) **copy-nit** → lege-voorstel wijst naar ↻ (09dbced). (d) **dagstrip-dot** vers-gepland = zone-gekleurd — bevestigd. **(e) OPEN (→ pass 4):** skip-reden "Iets anders gedaan" overlapt met de geplande Overridden-kaart → verzoenen bij pass 4.
- **VISUELE VERIFICATIE AFGEHANDELD** (Daan, /dev mobiel incognito): 1b + check-in + Schema 3a/3b/3c + AvailabilityEditor + app-header + DEZE WEEK · GEPLAND VS GEDAAN + type-schaal bevestigd. (Claude Code heeft géén browser → logische + `clasp`-compile-verificatie blijft de bouw-norm; Daan doet de visuele checkpoint.)
- **WeekLoad `stale`** = hardcoded `false` (F.3-signaal bestaat nog niet) — implementeren wanneer er een "plan verouderd"-detectie is.
- **"Doe iets anders" / WorkoutPicker** bewust NIET gebouwd in 3a (geen backing, geen dode knop) — future.
- **Dispositie-besluit:** `saveDisposition` doet bewust géén `generateProposal` (read-side overlay) om "inhalen" via debt te vermijden. Herzien als inhalen ooit wél gewenst is.
- **Dode CSS na Fase 3:** `.period-toggle`/`.vorm-strip-svg`/`.strip-*`/`.metric(s)`/`.lc-delta` ongebruikt (onschadelijk) — opruimen optioneel.
- **Token-reconcile (open):** `--chart-grid` = solide `var(--border-subtle)` i.p.v. de ontwerp-`rgba(255,255,255,0.05)` (Google Charts honoreert alpha niet) — met Claude Design afstemmen.
- **Garmin-push** nog week-niveau (geen per-dag `GarminSync`-knop) → Fase 6.
- **Badge-vocabulaire** ("Klim"→"Sweet Spot" e.d.) → Fase 4 (Trainingen-categorie-bron canoniek maken) / Claude Design. Niet-blokkerend; badge blijft intent-based (live toont al "Sweet Spot").
- **Fijnere zone-granulariteit** (>105% splitsen in Z5 VO2max / Z6 Anaëroob) = engine-werk; Z6 zeldzaam → geparkeerd.
- **IF blijft proxy** (√(TSS/(uur×100)), ~0,84 vs NP-IF ~0,92); blok-%FTP/watt = future server-calc.
- **VISUAL-FIDELITY — foundational pass DICHT** (Fase A 64cecdc + B 4991f54 + WeekLoad-sliver 1c8f367): type-schaal/leading geconformeerd, app-header (FTP COACH + ISO-weeknr + DK-avatar) live, DEZE WEEK · GEPLAND VS GEDAAN met /-noemers. De eerdere "te klein"-indruk was een **desktop-render-artefact** (mobiel = goed) → GEEN scale-bump nodig. Dag-detail-viz **NU GEDAAN** (2005c59 inline-metrics + zoneBar-restyle; 101cd2c power-per-blok via `structuur`/`wattsRange`) → **Schema + Vorm qua design compleet**.
- **Edge (full-7-save):** opslaan leidt E altijd af (nooit 'recovery') — geen issue zolang recovery fase-gedreven blijft (engine-only, F-beschikbaarheid §3).
- **TODO WeekLoad no-plan-guard:** `progressPct`=null als er geen plan is → de client moet "null%"/0-noemer-weergave vermijden (toon "—" of verberg de Voortgang-regel + noemers). Niet-blokkerend; onzichtbaar zolang er een plan staat.
- **TODO (optioneel) header pixel-fijnslijp** vs mockup: wordmark-weight 600→700, avatar-glow zachter/groter, skew-hoek/avatar-px. Niet aangevraagd; nice-to-have.
- **Test-gate = `node test-gate.mjs`** (autonoom): `clasp run-function runSelfTest` draait remote → CC verifieert nieuwe pure-calc-testcases zelf vóór commit (geen handmatige editor-run meer). `_Diag.gs` blijft ongetrackt voor Logger-diagnostiek; een diag die RETOURNEERT kan ook via run-function.
- **PIVOT — DESIGN-FIRST (grotendeels AFGEROND):** Schema + Vorm + Trainingen + Instellingen-drawer nu design-first gebouwd; van het functionele 7-pass-plan is **pass 4 (Overridden/FreeRide) gedaan** via de day-override. Resterend = pass 5–7 + adaptatie-executie (zie boven).
- **Knip (a) — VERIFIED + GEFIXT (e9ba7b7):** root cause van de 185→134 (en 200→161, week 24) TSS-drop: 'n GEMISTE / beschikbaarheid-uitgezette VOORBIJE dag kreeg `voorgesteldType` geblankt (snapshot-sync, Algorithm.gs) → de snapshot-guard `!d.voorgesteldType return` liet 'm vallen VÓÓR de knip-a-freeze. Fix = freeze-first via puur `snapshotDayAction_` (voorbij + vorige entry → bevries, ONGEACHT train/type; date-compare `dISO < todayISO` i.p.v. tePlannenSet; toekomst volgt 't nieuwe plan, nooit bevriezen). Wed 2026-06-10 hersteld in weekplan_2026-06-08 (deep-copy van 06-09 → 4 entries, 200 TSS / 4:50 / 4 dgn). +`testSnapshotDayAction_` (9, incl. de bug-case verbatim). Selftest 668/0.
- **Taper Deel 2 — gevolg `voortgangPct`:** door de ref-datum-fix meet de historische per-week fase-loop (`WebApp.gs:632`, voorbije weken) Recovery nu strikter per weekgrens (i.p.v. tot vandaag) → corrigeert een latente over-classificatie als Recovery; `voortgangPct` kan daardoor licht ZAKKEN (correcter). Niet-blokkerend.
- **Taper Deel 2 — B-week-presentatie:** een near-B zet `fase='Taper'` → plan-card/copy (`buildPlanModel_` `cur=macro.fase`, Proposal/WebApp `isTaper`) leest "Taper" terwijl `macroFase` Base/Build/Peak blijft. De DAG-toewijzing is correct (mini-taper alleen de laatste ≤3 dgn); alléén de week-kop is grof. `macroFase` is in de payload aanwezig → de kaart later laten tonen "Base · mini-taper voor {B}" = future refinement.
- **Carry-over — doel "Onderhoud":** het Instellingen-contract noemt een doel-modus "Onderhoud", maar de engine-`DOEL_OPTIONS` kent die niet → reconcile (label vs engine-key) bij de Instellingen-afmaak.
- **Dead-code (CLAUDE.md-lijst) OPGERUIMD** (3d1404c): `ifMetric_`, `.wk-sub .wk-metrics`, `.chart-caption`, readiness-`weight`-payloadveld. NB: `assignWorkouts`-param `eventDate` is sinds Deel 2 vestigiaal (taper leest `taperCtx.datum`) → losse opruim-kandidaat (niet op de oude lijst).
- **Adaptatie-executie LIVE (be33ef7) — open sub-items:** (1) **knop-stijl design-reconcile** — interim `--btn-primary-*`, maar C-bis zegt "geen knop" → een zachtere in-callout-stijl is wenselijk; (2) **UX-noot:** de suggestie-tekst ("Voorstel: …") blijft staan terwijl de knop verdwijnt bij `null`-target — copy/knop nog niet helemaal in sync; (3) **target-scope = binnen-week** → op zondagavond is er vaak geen toekomstige target meer → knop valt weg voor de hele week (cross-week-target = future); (4) **happy-path nog VISUEEL te bevestigen** (knop→tap→override op target→"Terug naar voorstel"→bron toont "ingepland op {dag}") — week 24. Dieper multi-dag-herbalanceren = later.
- **Coach-polish:** (1) dagstrip-dot override-kleuring **DONE** (db4e62b); (3) copy-nuance per substitutie-type **DONE** (dbaec81, drempel-credit). Resterend (2) workout-detail-watt-bloklijst slider-live → **GEBLOKKEERD, needs server-side** (zie "Item 3" hieronder).
- **Functioneel 7-pass-restant + Scope B blijven staan:** pass 5 Recovery "Toch trainen" (los van de override-picker), pass 6 per-dag **GarminSync** (vervangt de week-"Push naar Garmin"), pass 7 Niveau-tab afmaken + QA; Connect/Empty-states (één-user, revisit bij Scope B).
- **Coach honesty — B (eerlijke geplande IF) GEPARKEERD, niet triviaal:** een zone-afgeleide geplande IF (i.p.v. `cfIf_`=√(TSS/uur/100)) maakt de IF-regel + alignment-state eerlijk, MAAR duwt ritten 'different'→'deviated' (deze rit |ΔIF| 0,10→0,03) → dan valt 't in de generieke 'deviated'-copy en verliest 't Stage 1's volume-narratief. B vereist dus óók C's volume-besef **state-onafhankelijk** maken (vuren op 't duur/volume-tekort, niet op de state). Brede blast-radius → eigen meerdaagse pass. Gedeprioriteerd naar honesty-polish (narratief klopt al).
- **Sweet_spot → "Drempel"-label (gevolg Stage 1):** `coachIntentFromZones_` kent geen sweetspot-bucket. **NETTO WINST:** vóór Stage 1 toonde een correct-uitgevoerde sweet-spot-rit "Sweet Spot (gepland, type) vs Drempel (gedaan, zones)" = fantoom-"harder dan gepland"; nu "Drempel/Drempel" = terecht gelijk. Echte fix = intent-↔-Trainingen-categorie-vocabulaire béíde kanten canoniek (Fase 4). Display-only planned-relabel = val. Geen verkeerde narratieven, alleen grover label.
- **LAADTIJD — structureel klaar; rest = volgende sessie(s).** Open-flow nu 1 API-call + 1 read/grote-tab + gecachete library. **#1 volgende hefboom** (~1,9s onder snelle-CPU): `dashStatsFromActivities_`/`dashNiveauReeks_`/`dashActualsByDate_`/`dashBeginAnker_` doen vier losse full-passes over dezelfde ~489-rijen-Activiteiten-array (read is nu 1×, calc-passes niet) → consolideren naar één pass. Hoog-risico (4 fns, andere outputs) → eigen sessie + kalme meet-omgeving. **Laatste API-call killen:** `icu_zone_times` mee-syncen in de Activiteiten-tab → `computeZoneDebt_` uit Sheet → open-flow 0-API (variantie weg; sync-pad + schema-wijziging). **Diminishing returns:** 46.957-char payload trimmen; dubbele week-TSS-calc (`_statusWeekTss_`/`getWeekLoad_`) consolideren. **Meet-discipline:** GAS-timing is high-variance — meet op off-peak; gebruik `getTrainingLibrary_` §3 als pure-CPU-canary (~650ms kalm) om load te detecteren. **`_Diag.gs` blijft ongetrackt** (diagLaadtijd + diagLaadtijd2; §4-loop-reconstructie staat op x0/kapot maar irrelevant — N is statisch ~22).
- **Item 3 (slider-live wattlijst) — GEBLOKKEERD → needs server-side:** de WorkoutDetail-watt-lijst komt uit server-`structuur` (exacte per-blok-watt via `blokLijstStruct_`); de slider-herrekende segmenten dragen `{bucket,minuten,hoogtePct}` (`hoogtePct` = balk-hoogte, GÉÉN %FTP) + er is geen client FTP×zone%→watt-helper → een live watt-lijst vereist per-blok-%FTP additief in de segmenten (server). Eigen kleine pass; niet geforkt.
- **Override↔dispositie display-overlap (versterkt pass-4-item):** BEVESTIGD — een override SCHADUWT een onderliggende dispositie in de weergave; 't wissen van de override onthult de dispositie. `saveDayOverride`/`clearDayOverride` raken uitsluitend `override_<date>`, nooit `disposition_<date>` (insurance-check clean, geen bug). Instance van 't "Iets anders gedaan ↔ Overridden"-overlap → verzoenen bij pass 4.
- **Vorm A/B-keuze — BESLIST: Variant A live** (pakket-volgend, 248527d): diepe progressie woont op Niveau; Vorm houdt de W/kg-LevelCard (deck) + compacte VormLevelSummary + "Progressie →". Variant B (Vorm-niveau-grafiek + MetricRow) vervallen.
- **Tech-debt na Vorm-A:** dode fns `drawNiveauChart`/`renderNiveauHead_`/`setNiveauWindow`/`niveauReeksDelta_` (geen live caller meer; `NIVEAU_WINDOWS` BLIJFT — Niveau-tab gebruikt 'm) → optionele opruim. `state.vorm.niveauReeks` BLIJFT (server-input voor `niveauProgressie_`); `state.niveauTier`-payload mogelijk ongebruikt (onschadelijk). **Cadans-pakket-fold = PARTIEEL** — alleen STAP 1-secties (CadansMark/coachnaam/Vorm-A) gefold in `FTP-Coach-export.md` + `INTERACTIONS.md`; de rest van `design_handoff_cadans/` = latere stappen.
- **Niveau-tab v1-restnotities** (Fase-2-elementen §c/§d KLAAR — zie STAND): (a) tier W/kg-afgeleid (6 banden, design) vs live `niveauTier_` (niveau-5-banden) — beide bestaan, Niveau-tab gebruikt W/kg → vocab-reconcile-kandidaat; (c) v1 hergebruikt de maand-tijdbasis (`niveauReeks` + `NIVEAU_WINDOWS`) i.p.v. daily-730d → fijnere tijdbasis = future; (d) "laden" leunt op de globale loader.
- **MASTERFILE binnenkomend (Claude Design):** finishing touches — eerst te verwerken in de volgende sessie.
- **Rit-detail-cache** cachet óók de genormaliseerde output (zelfde antipatroon als power-curve vóór 46f3e1e); rauw-cachen als 't ooit speelt (niet urgent — rit-stats jaar-immutable).
- **Venster-switch:** alleen 90d/1y; meer vensters (42d/season/all) = `curves=<id>` uitbreiden ná een id-probe.
- **`ftpBandFromProjection_` gain-consts** (FTP_GAIN_PER_CTL_=0.004 / CAP_=0.08) = conservatieve placeholders, kalibreren.
- **goal-profile = alleen 'girona';** andere doelen = extra `GOAL_PROFILES_`-entries + `activeGoalProfile_`-mapping uit settings.doel/A-event (architectuur ligt er; per-event-dimensie-sets + doel-kiezer = latere fase).
- **Rider-type intern 'All-rounder'** (koppelteken) vs statische as-tekst 'Allrounder' — triviale opschoning.
- **Archetype-generator (fase 1) — open:** (a) DUUR-BEWUSTE intent-keuze + DEKKING-bewustheid → OPGELOST in fase 1b (C1 `intentHaalbaar_` duur-haalbaar-filter + C1b/forward coverage). (b) DocProps write→dashboard-keten = empirische restcheck (fase-afhankelijk Build/Peak); recency-seed is nu DIEP (`gatherWeekplanEntries_`, laatste 8 weken, 091f7c9). (c) Doel-tak (niet-event fallback) ook door `goalWorkout_` = kleine vervolgstap. (d) VO2max/Conditie eigen profielen (nu → default klim) + resterende generators (`genericLongZ2`/recovery/taper/pendel/combo) → archetypes migreren → fase 2 (heft de pool-vs-generator-tweedeling op; library toont dan alle archetypes). (e) `Proposal.gs` `macro.fase`→`macroFase`-harmonisatie (legacy faseOffset-nuance; web leest de autoritaire opgeslagen expansie). (f) Sweetspot-bucket-display-wrinkle (`sweetspot_long`→drempel / `sweetspot_short`→tempo, 1%-mid; cosmetisch, effectTag consistent). (g) `rec.zone` (int) vs `blok.zone` (bucket-string) naamgeving-opschoon. (h) Item 3 (slider-wattlist) GESHIPT (c9e9b45/1b98f0b; per-blok pctLo/pctHi → client `blokLijstSegs_` live).
- **Archetype-diversiteit — vervolg (post-9326fa7):** (1) **Base-quality widening** — Base staat hard op `sweet_spot` (`allocateQualityWeek_`); naar `goalWorkout_` verbreden geeft Base-variatie maar vereist fase-intent-tuning (anders drempel-zwaar) — eigen stap. (2) **Bibliotheek↔archetype-coherentie** — Trainingen-browser gebruikt `renderVariant_`-pools, NIET de 21 archetypes → twee datasets (grotere refactor; = (d) hierboven). (3) **Crit/race-vormen + crit-profiel** — bewust uitgesteld (klimdoel); architectuur (structuurtype incl. `race_sim` + profiel-laag) draagt ze. (4) **VO2max/duur-profielen** — alleen klim+ftp zijn echt (rest → default klim). (5) **Optioneel** — `archetypeVoorkeuren`-tuning voor de klim-sim-vormen + intent-balans (vo2-weging) bij ondervertegenwoordiging. (6) **`overrideKaart_`** (Script.html:2075) — zelfde latente scaled-segs/stale-structuur-mismatch bij verkorte overrides; `trnScale_`'s uniforme low-bucket-scaling balloont inter-rep rests bij lange duur (geparkeerd: extra duur → Z2-fill).

## Volgende stap
- **Archetype-diversiteit /dev-verify (OPEN, NIEUWSTE):** genereer + 2-3× regenereer een Build/Peak-week → per keer andere kwaliteitsvormen (rotatie + diepe seed); bevestig dat sandwich / 40-20 / over-unders correct renderen (watt-lijst + zoneBar). Engine-only → géén design-fold.

**FASE 1b — CODE-COMPLEET (zie STAND).** Resterend:
- **Fase 1b close-out** (Build/Peak-geblokkeerd): empirische /dev-verificatie van de week-plaatsing (demo-hook à la rdydemo, of wachten op Build/Peak). Engine-only → géén design-package-fold; export-noot voor de pendel-Z2-default + `reden` optioneel.
- **Follow-up #4 (weekend-aware avoid-hard)** — optioneel/laag-prio. Aanbeveling: laten liggen tenzij een live Build/Peak-week een geforceerde Sa-Zo-stapeling toont (vangnet is conservatief-by-design).

Daarna de bestaande sporen (ongewijzigd): STAP 2 verify + close-out (taper-geblokkeerd: /dev-verify + `rdydemo`-hook verwijderen + export §d/INTERACTIONS folden); Item 3 (slider-wattlist); laadtijd-consolidatie (hoog-risico); functioneel-restant (pass 5 Recovery "Toch trainen", pass 6 per-dag GarminSync); Scope B (Telegram-bot).

---

## Historie & engine-referentie (bruikbare historie — overleeft de restyle)

### KLAAR (done sinds bbd1a6b)
- **Niveau-tab v1 (a104a60 fold / e0eb120 server / ff26b04 client):** *Fold* — `_import-design-4` → Niveau-tokenblok verbatim in `design/tokens.css` + `src/Tokens.html` (identiek); §4-spec → `FTP-Coach-export.md`; interactie-spec → `INTERACTIONS.md`; 4 screenshots → `design/screenshots/`. Geen token-collisie (`--tier/wkg/traj/...` leiden af van bestaande base-tokens); `_import-design-4/` ongetrackt. *Server* — `ctlReeksMaandelijks_` (daily-PMC, 42d-tau, over Activiteiten-TSS idx8 → maand-CTL) + `niveauProgressie_` (`niveauReeks`+CTL → `{maand,niveau,wkg,ctl}`) → `state.niveauProgressie`. +9 `testNiveau_`-asserts. *Client v1* — vermogen-snapshot (FTP-hero · W/kg-pill · tier-chip · 6-rung ladder; tier W/kg-afgeleid via `NV_TIERS`) + progressie-kaart (`renderNiveauProg_`/`drawNvTraj_`: metric-switch Niveau/W-kg/Fitheid · venster 1M/6M/12M/Alles · periode-delta · gestippelde CTL-overlay via ComboChart dual-axis). Later-elementen (3 rijdersprofiel + 4 doel-projectie) = één minimale "Fase 2"-kaart, GÉÉN backing-logica. **Vorm ONAANGEROERD** (`drawNiveauChart`/`niveauReeks`/`renderVorm` ongewijzigd; aparte `nv*`-state + `state.niveauProgressie`). VISUEEL GEVERIFIEERD (Daan, /dev); selftest 224 te bevestigen.
- **Dispositie toggle-to-clear (6641b10, Script.html):** tik op de al-geselecteerde dispositie-chip → wist 'm. OPTIMISTISCH: lokaal `disposition=null`/`coach=null`, status→vandaag/gepland, `renderSchema`+`renderDagDetail` (×→dot, terug naar voorstelKaart), dan `saveDisposition(dateISO,null)` op de achtergrond ZONDER onState (instant; afwijking zoals pushWeb). Fallback = round-trip als 't lokale voorstel ontbreekt. ×-glyph + `title="Tik nogmaals om te wissen"`. **Bug-root:** de LIVE coach-missed-render `gemistDetailHtml_` had geen clear-affordance; alléén de fallback `gemistKaart_` had 'r al. Geen server-change, geen nieuwe token. Visuele bevestiging open.
- **Dagstrip-dot override-kleuring (db4e62b, Script.html):** `stripDots_` leest bij een actieve override (gepland/vandaag) de override-zone uit dezelfde bron als OverriddenDetail (library → `trnCatByType_(workoutType).zoneVar`; free-ride → intensiteit→bucket). Rust/streepje + dubbele-sessie-dots ongemoeid. Geen server-change, geen nieuwe token. VISUEEL GEVERIFIEERD (dot geel/Drempel over blauw/Duur, vóór replan).
- **Coach copy-nuance per substitutie-type (dbaec81, Coach.gs+SelfTest.gs):** in de substitutie-tak krijgt een DREMPEL-substitutie bij een endurance/klim-event klim-specifiek credit (geen "tilt niet op"), met eerlijke Z2/volume-trade-off. High-intensity-bucket = `vo2` (er is GÉÉN vo2max/anaeroob-bucket) → credit op drempel, `vo2` blijft niet-specifiek. Escalatie (pat≥2), `plIntent===acIntent`-volume-tak (Stage 1), 'deviated' en `adapt` ongemoeid. +3 `testCoach_`-asserts (in de bevestigde 215/0).
- **LAADTIJD-optimalisatie — 4 structurele passes (cdc2f91/851a51e/44fac0c/5c5805a, server):** her-meting via `_Diag.gs diagLaadtijd2` (tab-read-tellingen + per-stap + cold/warm + payload) weerlegde de "GÉÉN API"-aanname. **Pass 1 (cdc2f91):** 8 dashboard-lezers (Activiteiten 6, Wellness 2) kregen een optionele trailing-array-param (default = zelf-lezen → callers/SelfTest byte-identiek); `getDashboardState` leest elke tab nog 1× (`readActiviteitenValues_`/`readWellnessValues_`) en geeft door. **Pass 2 (851a51e):** `getTrainingLibraryCached_`-wrapper naast (ongemoeide) `getTrainingLibrary_`, CacheService.getUserCache, key = MD5(settings.ftp+lthr), 6h-backstop, zelf-invaliderend; library puur bevestigd (alleen ftp/lthr, geen datum/fase). **Pass 3 (44fac0c):** `_statusWeekTss_`+`getFormScore_` kregen een optionele array-param → lezen in de dashboard-flow week-TSS/CTL-ATL uit de GESYNCTE Activiteiten/Wellness-tabs i.p.v. live (zelfde rekenkunst, alleen bron). **GEDRAG:** form/week-TSS reflecteren nu laatste-sync — consistent met de rest van 't dashboard (al Sheet-gebaseerd); verse data via sync (↻/regenerateWeb). Andere callers (Proposal/Telegram/getReadinessScore-default) onveranderd (default = live). **Pass 4 (5c5805a):** `bepaalFaseVoorDatum_`+`buildPlanModel_` events-param; `getDashboardState` hoist één `getAllEvents_()` → top-level + voortgang-loop (×~22) + plan-kaart; Events-reads ~25→1. **Netto per load weg:** 6 Activiteiten- + 1 Wellness- + ~24 Events-reads + ~600ms library-hercompute (warm) + 2 API-calls. **runSelfTest 212/0** door alle 4 passes. **Patroon:** optionele-trailing-param + default-zelf-lezen/live = veilige read-once-thread-idiom (callers/tests byte-identiek).
- **Coach-classificatie Stage 2 — patroon-teller zone-consistent (0497611, server):** `coachPatternCount_` (WebApp.gs:369) leest `planByDate=dashWeekplanByDate_()` (snapshot draagt `blokken` Algorithm.gs:219 + `intent` :218 → zone-min ZONDER API). Planned-side: `intentFromType_(workoutType)` → `segmentsFromBlokken_(p.blokken)||segmentsFromIntent_(p.intent)` → `coachZmFromSegs_` → `coachIntentFromZones_`, type-fallback bij lege segmenten (byte-identiek Coach.gs:189). Done-side (`intentFromIF_`) ongemoeid; rank-vergelijking op dezelfde intent-schaal. Effect: geplande drempel-dag = 'drempel' → buiten {duur,herstel} → vals-positieve substitutie-tik weg; echte easy-dag harder gereden tikt nog. Geen extracted helper → geen nieuwe pure calc → geen nieuwe testcase (Stage 1's `fa`/`fb2` dekken de route).
- **Coach-classificatie Stage 1 — planned-intent uit zones + zelfde-intent-narratief (eb8e718, server+test):** **(A)** Coach.gs:189 `plIntent = coachZmFromSegs_(planned.segmenten)→coachIntentFromZones_` (zelfde significantie + rust-exclusie als de done-kant) `|| intentFromType_`-fallback bij lege segmenten — vervangt 't type-label dat 't geplande 3×8'-Z4-blok negeerde; reading-"Type"-gepland relabelt mee (:194/Script.html:456). **(C)** `coachCopy_` :168 (top 'different'): guard `if(plIntent===acIntent)` → "prikkel geraakt, korter gereden, minder Z2/totaaltijd", event-bewust; géén intensiever/lichter, géén swap, géén patroon-escalatie, `adapt:null`. 'deviated' ongemoeid. +6 `testCoach_`-asserts. **Drift:** echt-gepland `sweet_spot` → label "Drempel" (zie Open punten, netto winst). **B (eerlijke geplande IF) = aparte pass** (geparkeerd).
- **Rit-detail 6-tegel-grid + W/kg + ZoneCompare (f5a214c, server+client+test):** `rideSheetHtml_` = 6 `rdMetric_`-tegels (VI + losse Max-HR-tegel + Calorieën weg, óók uit `rideDetailModel_`); server-veld **gewicht** (`detail.icu_weight`→`hit.icu_weight`→`getGewicht()`) + pure rdMicro **`rdWkg_(gemW,gewicht)`** (1 dec, null-guard, uit gem. vermogen — niet NP); `rdMetric_` optionele sub-param; hero NP·IF·TSS + intervallen + curve-placeholder ongemoeid; dode `.rd-zones`/`.rd-zbar`-CSS (onschadelijk). **ZoneCompare** = `coachZonesHtml_` één rij per zone (Z1–Z5; Z6 leeg door >105%-merge) **DoneDetail-only**; `tizBar_` (rit-detail) ongemoeid. CC bundelde A+B in één commit (gedeelde Script.html). +4 `rdWkg_`-asserts.
- **Design-import ZoneCompare + rit-detail (c1f61f8, two-file mirror):** `--zcompare-track`=`var(--reading-track)` / `-plan-strength`=26% / `-track-h`=18px / `-done-h`=8px / `-label`=`var(--text-secondary)` / `-tag-unplanned`=`var(--align-different)` / `-tag-skipped`=`var(--text-muted)` verbatim in `design/tokens.css` + `src/Tokens.html` (identiek geverifieerd); export §1bis/§1ter bijgewerkt; INTERACTIONS gefold; frames 08-11 in `design/screenshots/`; `_import-design-3` verwijderd.
- **Bottom-nav-iconen (02e7a31, client):** de 4 placeholder-emoji's (📅📈📚📊) vervangen door inline `currentColor`-SVG line-iconen (Schema=kalender / Vorm=trendlijn / Trainingen=gestapelde balken / Niveau=staafgrafiek) uit `01-nav-final.png`. `.nav-ico` 23px; kleur getokeniseerd `--tabbar-icon(-active)` + `--tabbar-border`, + `--tabbar-indicator`-balkje op de actieve tab. `switchTab`-wiring + labels ongemoeid (icoon erft de knop-kleur). Géén icon-assets in de design-map → narekend uit de screenshot.
- **Rit-detail v1 (60254ca, server+client+test + Claude Design-import §1ter):** `getRideDetail(dISO)` resolveert de intervals.icu-activity-id via match-by-date in `getActivities` (`hit.id`; patroon = `getDayCoachZones`), fetcht `/activity/<id>` + `/activity/<id>/intervals` via `intervalsRequest_` (Basic auth, LETTERLIJKE activity-id — niet de `{id}`=athlete-placeholder). **%FTP per interval = computed** `round(watt/ritFTP×100)` → `pctZoneBucket_`, met de FTP die VÓÓR de rit gold (detail/hit `icu_ftp` → settings); native-`/intervals`-%FTP-probe = toekomst. **Cache** DocProps `ridedetail_<activityId>` (jaar-immutable, lazy, alleen na geslaagde detail-fetch); client-cache per rit. Client: **"Bekijk ritdetails"** op DoneDetail/`actualKaart` → `openDrawer`-sheet; states **loading-skeleton / geladen / RideError+opnieuw**. Opbouw: kop · **TimeInZoneBar** (dedicated `tizBar_`, flat proportioneel — NIET `zoneBar`) · hero NP/IF/TSS · metric-grid · zone-gestripte intervallen (duur mm:ss) · vermogenscurve-placeholder (fase 2). Tokens `--ride-*`/`--tiz-*`/`--interval-*`/`--skeleton-*`/`--state-*` gemirrord (design/tokens.css + src/Tokens.html). Pure helpers `rdPctFtp_`/`rdDurMs_`/`rdDurHms_` + `testRideDetail_` (+12 asserts). **Drift:** tijd-in-zone = flat bar (niet `zoneBar`); interval-duur mm:ss (niet "8m3s").
- **Adaptatie-executie (be33ef7, server+client+test):** pure **`coachAdaptatie_`** (Coach.gs) synthetiseert uit planned-intent → engine-type (`COACH_INTENT_ENGINE_TYPE_`) + **echte** `getTrainingLibrary_`-variantId + ingekorte duur (70% / 15-min-stap / 45..origineel) + de eerstvolgende plannbare toekomstige dag een `saveDayOverride`-payload `{dISO,type:'library',workoutType,variantId,durMin,from,label}` | `{planned:true,dISO}` | `null`. `getDashboardState`-post-pass koppelt elke coach-suggestie (afgeweken/gemist, waar `coach.adapt` staat) aan een target. **"Plan deze aanpassing"-knop** in DoneDetail(afgeweken)/GemistDetail (alleen bij `adaptatie!=null`) → `saveDayOverride` → `onState`; **idempotent** via override-`from`-tag (madeFrom → bevestiging, geen dubbel-plant); omkeerbaar gratis uit 5655565. Library-tak only. Test `testCoachAdaptatie_` (+11). **Drift:** knop NIEUW (C-bis = "geen knop") → `--btn-primary-*` interim (design-reconcile open).
- **Coach-feedback dag-niveau — CORE (f795b74) + fixes (7364ca4), server+client+test:** nieuwe **`Coach.gs`** (PUUR): classificatie (`intentFromType_`/`intentFromIF_`/`coachIntentFromZones_`), alignment (`coachAlignment_` — relatief op IF+TSS zodat trouwe uitvoering 'op plan' telt), impact + **doel-bewust, gedifferentieerd narratief** (`coachCopy_`: richting intensiever/lichter/ander × fase × **event-demand** + **patroon-escalatie** ≥2 intensiteit-subs). `getDashboardState` zet per voltooide/gemiste dag een `coach`-object (event-ctx via `coachEventFromMacro_`, patroon via `coachPatternCount_`). Client: **DoneDetail** (alignment-chip + gepland-vs-gedaan reading-tabel + dubbele zone-bars + "volgt plan %"-balk + COACH-callout) + **GemistDetail** (neutrale chip + skip-reden + COACH·IMPACT). **IF-BUG root cause = `Sync.gs:157`** (intervals.icu `icu_intensity` als percentage opgeslagen, bv. 77 voor 0,77) → de coach las 'm rauw → classifier zag 77 → álles VO2max + display "77,09". **`cfNormIf_`** normaliseert nu overal naar 0–1 (display + classifier + alignment). **Reële time-in-zone** via `getActivities` + `icu_zone_times` (lazy `getDayCoachZones`, client per dag gecachet; `getDashboardState` blijft API-vrij) → Gedaan-bar = échte zones (Z2-basis + Z4/Z5-blokken), intent uit de zwaarste significante bucket (`coachIntentFromZones_`); IF-benadering = fallback. Girona/trip = endurance → duur/drempel-specifiek (intensiteit-substitutie = niet-specifiek, eerlijke trade-off, géén onjuiste "aerobe basis"-claim). Test: **`testCoach_`** (IF-normalisatie, zone-intent, doel-bewuste tiers + patroon). planned/override-tak ongemoeid.
- **Coach-feedback DESIGN (Claude Design import → f795b74):** frames `08-dag-voltooid-match` / `09-dag-voltooid-afwijking` / `10-dag-gemist` bevestigd; nieuwe tokens **`--align-*` / `--reading-*` / `--coach-*`** verbatim gemirrord in `design/tokens.css` + `src/Tokens.html` (géén root-`tokens.css` — bestaat niet, two-file mirror). Export **§1bis** + **INTERACTIONS C-bis** gefold. Drift: AANPASSING = suggestie ("Voorstel: …"), %-balk alleen bij zelfde-intent.
- **Day-override end-to-end (5655565, server+client+test):** `saveDayOverride(dISO,{type:'library',workoutType,variantId,durMin}|{type:'free',ritType,intensiteit,durMin})` + `clearDayOverride` → DocProp `override_<date>` (spiegelt `saveDisposition`; geen replan → directe toon). **Respect-op-replan:** `generateProposal` bakt een override-dag in via `buildOverrideWorkout_` (specifieke variant of free-ride-synthese met zones/TSS) → één proposal-key + weekplan-entry; ook op een **niet-train-dag** (forceert zonder het planner-model te muteren). `state.plannedTypes` unieert overrides. Client: **OverriddenDetail** (pin + echte WorkoutDetail) / **FreeRideCard** + "Terug naar voorstel"; **"Andere training kiezen"** → **WorkoutPicker-overlay** (home → bibliotheek-views → Kies workout; vrije-rit-form). Trainingen-"Inplannen" → `saveDayOverride` op de eerstvolgende plannbare dag. Test: `testDayOverride_`. **Drift:** pin = `--mode-chip-*`; "Terug naar voorstel" (contract §127); dagstrip-dot nog niet override-gekleurd; detail-watt-bloklijst niet slider-live.
- **Trainingen-tab t/m workout-detail (7212a65, server+client+test):** read-side **`getTrainingLibrary_(settings)`** bouwt {categorie→varianten[]} via de bestaande pure builders (`renderVariant_`/`segmentsFromBlokken_`/`tssFromZoneMinutes_`) → `state.library` + `state.plannedTypes` (In-je-blok-badge). Client: status-deck (hergebruik readiness/level + geparametriseerde `syncDeckDots_`/`wireDeck_`) + view-statemachine **cats→category→workout**, CategoryCard (zone-marker `--zone-*`), **DurationSlider** 45–240/15 met live intent-recompute (extra duur → Z2), VariantRow + WorkoutDetail (`zoneBar`/`zoneBlock_`/`inlineMetrics_`), **"Inplannen"**. Test: `testTrainingLibrary_`.
- **Taper-model Deel 1+2 + dead-code (autonome batch: 185e9d6 deel1 / e959d35 deel2 / 3d1404c dead-code, server+test+client):** event-nabijheid wordt nu **vanaf vandaag** gemeten (niet de week-maandag). Nieuwe PURE helper `eventFase_(events, refDate)` (Doel.gs) = ÉÉN bron voor de fase-mapping → `{ fase, macroFase, hoofdEvent, taperEvent, taperVenster, dagenTot, wekenTot }`. **macroFase** = periodisering van 't A/trip-hoofdevent (Base≥9wk/Build≥5/else Peak), **LOS** van de taper. **Taper-overlay:** A/trip ≤ `A_TAPER_DAGEN` (7) → venster 7 (volle taper-week); anders dichtstbijzijnde B met 0..`B_TAPER_DAGEN` (3) → venster 3 (mini-taper); **C nooit**. `fase = Recovery > Taper(taperEvent≠null) > macroFase`; een near-B drijft de taper maar **NIET** de macro. `bepaalFaseVoorDatum_` kiest ref = vandaag voor de huidige week, anders de week-maandag (historische voortgang-loop WebApp.gs:632 + +1-planning blijven correct) en exposeert `macroFase/taperEventDate/taperVenster/taperIsTrip` (superset-return, alle lezers blijven werken). **Per-dag-gating GEBOUWD** (NIET deferred): `assignWorkouts` krijgt `taperCtx={datum,venster,isTrip}` + de onderliggende `macroFase`; een dag tapert ALLEEN als hij 0..venster dagen vóór 't taper-event ligt — anders (en post-event) normale toewijzing volgens macroFase. `buildWorkout` krijgt nu ook de onderliggende `macroFase` (taper-workouts blijven type-gedreven). Test: `testEventFase_` uitgebreid (A@7d→Taper venster7, A@8d→Peak, ver-A→Base, A-race-deze-week→Recovery, geen-event→null, B@3d→Taper venster3 macro Base, B@4d→geen taper, C@1d→geen taper). Client `planCardHtml`: kop "nog X dagen" als dagenTot<14, anders weken. **Dead-code opgeruimd** (CLAUDE.md-lijst, elk 0 live refs geverifieerd): `ifMetric_`, CSS `.wk-sub .wk-metrics` + `.chart-caption`, en 't ongebruikte `weight`-payloadveld in de readiness-factoren. **Daan draait `runSelfTest` handmatig.**
- **Instellingen-drawer (Fase 1 fad09e3 + events-CRUD Fase 2 63d44c9 + bewerkbare event-details a523c81, design-first uit INTERACTIONS):** avatar (app-header) → opent de drawer-variant van de overlay-primitive. **5 secties** + **Events**-sectie (CRUD: toevoegen/verwijderen, prio-cycle A/B/C, type race/trip, bewerkbare details) met **één Opslaan** (`saveSettingsEditor` ket `saveSettings`→`saveEvents`). `saveSettings` schrijft Sheet-cel + DocProp + flush (GÉÉN api-key — Koppelingen = read-only, security); `saveEvents` schrijft Events rows 3-12 + `events_json` + `avail_dirty`. **Koppelingen-screenshots `settings1/2/3.png` waren mislabeld** (= Vorm-tab) → gebouwd uit het INTERACTIONS-contract.
- **Knip-fix (b) — volgende-week plannen (d0ad545, server+client):** editor-scope **"Volgende week"** (3e knop; "Hele week"→"Deze week") schrijft de bestaande **Weekplanner +1**-tab via nieuw `saveAvailabilityPlus1` (roept `ensureCurrentWeekPlus1` eerst; zet BEWUST géén `avail_dirty`) + payload-veld `availabilityPlus1` (`readAvailabilityPlus1_`). Client `availLoad_`/`availLoadedWeek` laadt/routeert per week-scope, behoudt edits binnen dezelfde week, bevestigt "Volgende week opgeslagen". +1 rolt maandag in via bestaand `_pullPlus1IntoCurrent_`. Preview = alleen minuten (bestaand `previewMin`). **NB:** het +1-PLAN (workouts) wordt pas bij de maandag-roll-in gegenereerd → hangt af van die-week-vorm/actuals; de +1-beschikbaarheid is wél stabiel.
- **Knip-fix (a) — snapshot bevriest voorbije dagen (0a74ee2, server):** de weekplan-snapshot-lus (`generateProposal`) herbouwt alléén `tePlannen` (datum≥vandaag); voltooid/missed/voorbije dagen behouden hun vorige `weekplan_<maandag>`-entry (gelezen in `prevByDate` vóór overschrijven) → een beschikbaarheid-edit verlaagt niet meer retroactief de geplande-cijfers van voorbije dagen. Defensieve fallback (geen vorige entry → herbouw). Hergebruikt de bestaande datum-split, geen nieuwe calc.
- **Design-conformance batch 1+2 (2b497f8 / c8212f3) + spacing/format-polish (3426460 watt-format / 9d6b768 + 8377b29 spacing) — visueel, geen server-calc:** Vorm niveau-grafiek in een `.rc-card` (§2c, niet meer zwevend; section-kop/caption weg); ReadinessCard factor-`weight%` weg; Schema dagstrip **vandaag-tint-rand** (`color-mix(--accent 45%, --border-subtle)`) i.p.v. `●`; Beschikbaarheid-knop per-dag onder de dag-detail (§102, opent voor `selectedDate`) + top-spacing `--s-7`; LevelCard-balk = **blok-voortgang** (`voortgangPct`) i.p.v. niveau/50, niveau = het getal (§2b/INTERACTIONS §194).
- **Dag-detail-viz + power-per-blok (2005c59 viz / 101cd2c power):** dag-detail = compacte **inline-metrics** ("X min · IF Y · TSS Z", `inlineMetrics_`) i.p.v. 3-koloms grid + `zoneBar`-restyle (hogere staven/gaps/rounded, width∝duur) + "Blokstructuur · N blokken"-teller. **Power-per-blok:** bestaande `structuur` (label/duur/watt-range via `wattsRange`, = ZWO/Garmin) gesurfacet naar de `weekplan_<maandag>`-entry → `blokLijstStruct_` rendert label·duur·watt (display "242–261 W"); fallback naar segment-render bij ontbrekende `structuur`. `wkMetric_`/`.wk-metrics` ongemoeid (gedeeld met Vorm-MetricRow).
- **WeekLoad-sliver — DEZE WEEK · GEPLAND VS GEDAAN (1c8f367) — GEVERIFIEERD /dev:** stats als gedaan/gepland (TSS / uren-h:mm / dagen, kleinere gedempte noemer `.wl-den`) + "Voortgang X% van plan"-regel boven de balk. Server `weekLoad` += `gedaanMin`/`gedaanUur` + `geplandMin`/`geplandUur`/`geplandDagen` via 2 PURE fns (`hhmmFromMin_` → "H:MM"; `weekPlanSummary_` uit `weekplan_<maandag>` — Σtss/Σminuten/distinct dagen, multi-sessie=1 dag). `tss`/`geplandTss`/`progressPct` (0–100, null=geen plan) ongemoeid. Overline "Deze week · gepland vs gedaan" (uppercase via CSS).
- **Visual-fidelity Fase B — globale app-header (4991f54) — GEVERIFIEERD /dev (alle tabs):** één `<header id="appbar">` vóór de secties (buiten switchTab, sticky, full-width, inhoud op `--s-4`): links ▌FTP COACH (skew-balkje `--accent-grad` + wordmark `--fs-h1`), rechts "Week N" (ISO-kalenderweek, client `isoWeek_` pure date) + DK-avatar (`--accent`-rand + `--accent-ring`-glow) → `openSettings` stub (Fase 5). Beide per-tab `.hdr` + alle `.hdr*` CSS + de `#schema-datum`-JS verwijderd; dag-detail-weekdaglabel intact.
- **Visual-fidelity Fase A — type-scale + spacing-conformance (64cecdc) — GEVERIFIEERD /dev mobiel:** Styles.html-sweep hardcoded px → BESTAANDE tokens + `html,body` base-rule (`var(--fs-body)`/`var(--lh-body)`; system-font weg → Alias `--font-sans`). ~12 font-size → `--fs-*`, ~30 off-grid spacing (10→`--s-3`, 14/18→`--s-4`), 2 off-palette kleuren (#1e293b→`--border-subtle`, #cbd5e1→`--text-muted`). `.hdr*` overgeslagen (Fase B verwijderde ze). GEEN scale-herontwerp — tokens bestonden al; "te klein"-indruk was desktop-render-artefact (mobiel = goed). On-grid/sub-grid literals + dode legacy-blokken bewust gelaten; `.loading/.error` off-palette geflagd.
- **Schema deel 2 pass 3b — AvailabilityEditor globaal → per-dag scoped (00820ab) — VISUEEL GEVERIFIEERD:** `#btn-avail` → `openAvailEditor` opent een overlay-sheet (`openDrawer`, zoals check-in). Entry-chooser scope Dag|Week via `--segment-*`; scoped `DayControls`: Train/Pendel als `--toggle-*`-switch, minuten-slider `--slider-*` (GELOCKT 30–360/step 15). "Alleen deze dag" muteert het slot van de dagstrip-geselecteerde dag (weekdag-index); "Hele week" = 7-rij grid. Opslaan bouwt ALTIJD de volle 7-array client-side → `saveAvailability` (WebApp.gs:670) ONGEWIJZIGD; `avail_dirty` + stale-banner + `onState` behouden. Oude `renderBeschikbaarheid`/`saveAvail`/`availRowSync`/`availMinSync` verwijderd. Pendel→E hard per F-beschikbaarheid §3 (pendel→'pendel', anders Za/Zo→'weekend', anders 'vrij', nooit 'recovery'). Geverifieerd: Train-uit→rustdag (Za), Pendel-aan→pendel-rit (Zo), full-7 intact, save→re-plan. **Ontwerp-afwijkingen:** (1) slider 30–360/15 = Daan-lock (binnen server-cap 600), ondanks INTERACTIONS §165 (30–240/15) + F-beschikbaarheid §4 (max=600); (2) entry-chooser = blijvend `--segment-*` i.p.v. INTERACTIONS §162-163 (tiles + "Terug", 2-staps) → **CLAUDE-DESIGN-RECONCILE-NOOT** (ontwerp intern inconsistent: tokens.css `--segment-*` vs §162-163 tiles; open, niet-blokkerend, `--segment-*` behouden); (3) plaatsing = overlay-sheet (volgt INTERACTIONS §102 "Beschikbaarheid"-knop).
- **Full-width layout-fix (1c0568f):** app is MOBIEL-ONLY (export `FTP-Coach-export.md:7` = 390×844, géén content-frame/max-width in het ontwerp). `#app` + `.bottomnav` `max-width: 480px → 100%`; `.body` padding `14px → var(--s-4)`. Pagina-content + overlay-sheet aligneren nu beide op `--s-4` (16px) horizontale marge (`.overlay-content` had al `--s-4`). `.hdr` houdt 18px horizontaal (2px-nicety, geparkeerd). Alleen layout/breedte, geen functionele change.
- **Schema deel 2 — zone-naming naar het 6-zone-ontwerp (2b1c893 namen / 36431fc VO2max→`--zone-5`):** `ZONE_LABEL`-waarden = zone-NAMEN i.p.v. Z-nummers (1 Herstel · 2 Duur · 3 Tempo · 4 Drempel · 5 VO2max); `zoneLegend_` + `blokLijst_` delen die bron, `'Z2'`-fallbacks → `'Duur'`. Top-bucket (anaeroob >105% FTP) `--zone-6`→`--zone-5` op 6 sites (`ZONE_VAR` + `INTENT_BADGE` vo2max/vo2_short/medium/long/3015/microbursts/pendel_vo2/combo_z2_vo2) → loste de kleur/label-tegenstrijdigheid op (`--zone-6` = Anaerobic-kleur, label was "VO2max"). `--zone-6` nu ongebruikt (token blijft). **Commit 2 "badge→categorie-namen" OVERGESLAGEN:** §1d spect "zone-badge (`--zone-*`)" = kleur, niet tekst; de categorie-namen horen bij Trainingen-`CategoryCard` (INTERACTIONS.md:220) + Doel-grid (:258), niet de Schema-badge → badge blijft intent-based.
- **Schema deel 2 pass 3a — carry-over-bundle (445f5b3 badge / f0087ad regen / 09dbced copy) — VISUEEL GEVERIFIEERD:** (a) **badge** = workout-intent-categorie via client `intentBadge_(voorstel.type)` (`INTENT_BADGE`-map) i.p.v. `primaryBucket_` → loste "Z4 op Lange Z2" op; onbekende token → fallback `primaryBucket_`. (b) **regen/actuals-fix:** `regenerateWeb` roept nu EXPLICIET `syncAll()` vóór `generateProposal` → verse actuals + wellness + `last_sync`-stamp, dán replan, dán verse payload (geverifieerd: ↻ → `last_sync` 13:24→14:34). (c) **copy-nit:** lege-voorstel-tekst wijst naar de ↻-knop.
- **Schema deel 2 pass 2 — WeekLoad regen-refresh + stale-banner + laatst-gesynct + zone-badge-fix (84f6899) — VISUEEL GEVERIFIEERD** (end-to-end: availability-change → banner → ↻-regen → nieuw Za-voorstel → banner weg)**:** ↻ (`refreshWeekLoad`) roept nu `regenerateWeb` i.p.v. `refreshWeek` (sync-only). **CORRECTIE (opgelost in pass 3a / f0087ad):** de oorspronkelijke claim "actuals-pull behouden" was FOUT — `regenerateWeb`→`generateProposal` draait `ensureDataAndReconcile_` (Algorithm.gs:574) dat alléén-als-leeg sync't, dus op een gevulde tab pullde ↻ GEEN verse actuals en verfriste `last_sync` niet. f0087ad voegt een expliciete `syncAll()` toe vóór de replan (geverifieerd: `last_sync` 13:24→14:34). Losse "Regenereer voorstel"-knop (`btn-regen`/`regenWeek`) verwijderd; ↻ neemt 't over. **Stale-banner** via `avail_dirty`-DocProp (`saveAvailability` zet '1', `regenerateWeb` wist) → `weekLoad.stale`, tekst "Je plan is verouderd t.o.v. je beschikbaarheid — werk bij". **Laatst gesynct** = `last_sync` (syncAll, `dd-MM-yyyy HH:mm`) → `weekLoad.lastSync` → `.wl-sync`. **Zone-badge** `primaryBucket_` = zwaarste bucket met significante duur (≥max(8min,12%), rust uit) → "Lange Z2"→Z2, intervallen→interval-zone (fix Z4-op-Z2). Blokstructuur-summary: "Blokstructuur ⌄"-chevron. NB: server `refreshWeek` nu ongebruikt (harmless).
- **Schema deel 2 pass 1 — dag-detail-display + glyph + check-in-migratie (90c807f checkin / f2f6e4f display):** dagstrip rust-glyph gefixt — `renderSchema` checkt nu `!voorstel && !actual` (streepje) vóór de `vandaag`-tak → vandaag-rustdag toont streepje i.p.v. `●`. Dag-detail §1d additief: **zone-badge** (`primaryBucket_`, `--zone-*`), **ZoneLegend** (`zoneLegend_`), **inklapbare Blokstructuur** (`zoneBlock_` = `<details>`: zoneBar+legend = klikbare summary → bloklist uit `voorstel.segmenten`), **IF-metric** (`ifProxy_` = √(TSS/(uur×100)); client-display uit tss+duurMin → géén server-calc/gate). Metrics nu duur/IF/TSS (actualKaart: RPE-tegel → IF; RPE blijft rating-widget). Check-in: `getTodayCheckin_` mapt oude `oké` → `matig`(slaap)/`normaal`(benen) op read.
- **Schema deel 1 — conformance (9fff4f0):** dagstrip-dots nu **`--zone-*`-getokeniseerd** (`stripDots_`, bucket→zone; 2 dots bij dubbele sessie) i.p.v. server-hex; rust = streepje `--border-strong` (`.m-rust`); selectie = `--accent`-rand + `--accent-soft`-vlak + `--accent`-cijfer; datums `--font-num`. Legacy `.card`/`.chip`/`.avail-box`/`.act-status` gemigreerd van alias-vars → nieuwe tokens (visueel identiek; alias bridged al, maar nu echte `--card-shadow`/`--border-subtle` etc.). Check-in: midden-labels per ontwerp (slaap `matig`, benen `normaal`; reorder best→worst), Vastleggen **disabled tot alle 3** (`checkinAllSet_`, default `null`), **"Later"/"Annuleren"-knop**. Server `CHECKIN_LEVELS`/`CHECKIN_QUESTIONS` keys hernoemd (`oké`→`matig`/`normaal`; oude check-ins → delta 0, graceful); `testCheckinDelta_` bijgewerkt. Behouden: live `×`(gemist)/`~`(preview)-markers (lot = deel 2-beslissing).
- **Fase 3 deel 4 — Vorm swipe-deck + LevelCard-fixes (ad552e5 tier+gate / 632662a deck) — Vorm-tab COMPLEET:** ReadinessCard (slide 1) + LevelCard (slide 2) in horizontale scroll-snap-`deck` (`.deck`/`.deck-slide`, Index; mounts `#vorm-status`/`#vorm-level` ongewijzigd) + dots `#vorm-deck-dots` (`syncDeckDots_` volgt scroll, dot-klik smooth-scrollt) per INTERACTIONS §deck. LevelCard: chip = **niveau-tier** via PURE server `niveauTier_(niveau)` (0–14 Beginner/15–24 Gemiddeld/25–34 Gevorderd/35–44 Vergevorderd/45–50 Elite → `state.niveauTier`; **runSelfTest-gate** `testTier_` 8 grenzen) + `--accent`-dot; delta = client `niveauReeksDelta_` (laatste−eerste `niveauReeks` = ZELFDE als grafiek-Alles-window → +1,9/+2,1 gereconcilieerd) "+x,x ↑ sinds {beginLabel}"; "X% van je blok behaald" uit `voortgangPct`. `state.niveauDelta` nu ongebruikt (payload, onschadelijk).
- **Fase 3 deel 3 — Conditie Balans-meter (f8f1784 trim / 845e7a8 feat):** legacy conditie-cluster VOLLEDIG verwijderd — `drawVormChart` (PMC), `drawVormStrip` (strip), `renderVormStats` (volume-tegels), `vorm-huidig`-block (Vorm/CTL/ATL + ramp/FTP), `#period-toggle`/`statPeriod` (7d/28d/jaar); mounts `#vorm-chart`/`vorm-strip`/`vorm-huidig`/`vorm-stats` weg (geen dangling refs). Server: `vorm`-payload getrimd tot `{niveauReeks, huidig}` (reeks/stats/spanDagen/eersteDatum/ftp/macroFase/rampBuildMin/garminVerdict/event + top-level `maandTotalen` weg; lokaal nog berekend voor ctlRef/voortgang/readiness). NIEUW `renderBalans_` (mount `#vorm-balans`): custom **SVG-track** (3 `-soft`-banden bad/good/fresh, schaal −30…+25, grenzen −10/+5, nul-tick `--border-strong`) + **HTML-marker** (`--fresh` + 3px `--bg-surface`-ring + 1px `--fresh`-stroke) op `vorm.huidig.vorm` (TSB, geclampt) + groot band-gekleurd TSB-getal + band-chip + "TSB · vorm-saldo" + onderlabels. Empty (geen `huidig`) → opbouw-melding. GEEN Google Charts → `-soft`-banden renderen correct via `style="fill:var()"`. Geen gate-case. Dode CSS blijft (onschadelijk).
- **Fase 3 deel 2b — gridline render-fix (bf98957):** `vAxis/hAxis.gridlines.color` was al gewired naar `cssColor_('var(--chart-grid)')` (prompt-hypothese "niet gewired" weerlegd). Hard-wit kwam doordat Google Charts de `rgba(255,255,255,0.05)`-alpha NIET honoreert (rendert opaque/bright) + `minorGridlines` stond ongezet (default-licht). Fix: `--chart-grid` → solide `var(--border-subtle)` (beide token-bestanden) + `minorGridlines:{color:'transparent'}` + `baselineColor`=`--chart-grid` op beide assen van `drawNiveauChart`. **Token-vs-render-afwijking:** ontwerp wil 5% white; Charts dwingt solide af (ontwerp-token later reconciliëren). `drawVormChart` (PMC) erft de solide major-gridline via het token; minor niet apart gefixt (verdwijnt in deel 3).
- **Fase 3 deel 2 — niveau-grafiek §2c afgemaakt (5f9f77d fix / 0b0ddb9 feat; 1e pass 0301fa8/b72db9c):** `drawNiveauChart` = **AreaChart** (accent-area `areaOpacity:0.26`, lijn `lineWidth:2.5`, "nu"-punt via style-role fill `--accent`), x-labels `--text-secondary`, y-as `--chart-axis`, gridlines+baseline beide assen via `cssColor_`. **Gridline-fix:** `--chart-grid` = `rgba(255,255,255,0.05)` (literal, beide token-bestanden) — de eerdere `color-mix(white)` gaf via getComputedStyle een `color()`-vorm die Charts niet parsete (+ hAxis stond op Google-default ~`#CCC`). `.seg` segmented `1M/6M/12M/Alles` → `niveauReeksWindow_` slice `-2/-7/-13/all` (default Alles); value/delta-header `renderNiveauHead_` (value = laatste-in-venster NL-komma; delta = laatste−eerste: "+x,x ↑"/"−x,x ↓"/"±0,0", `--good/--bad`); empty (<2 punten) → "—" + geen toggle. Mount `#niveau-head`; los van `#period-toggle`/`statPeriod`. Geen server-calc → geen gate-case. **Google Charts-limieten:** geen gradient-fade (flat 26%), lijn-caps niet rond, "nu"-punt zonder stroke-ring. **Gereconcilieerd tegen `INTERACTIONS.md` §Vorm-analyse** (toggle READ·💻 + delta-redraw + verborgen-bij-eerste-keer; NiveauChart → EmptyChart bij eerste keer) — geen deviatie. NB: LevelCard hoort per INTERACTIONS.md in een swipe-deck (kaart 2) → deel 4.
- **Fase 3 deel 1 — Vorm LevelCard + MetricRow + chart-tokenisatie (e8fbbb1 tokens / 2d2f356 feat):** `levelCardHtml(state)` (niveau/50 + chip `beginLabel` + W/kg + `--accent-grad`-balk + `niveauDelta`; rc-skin; mount `#vorm-level` onder ReadinessCard) + `metricRowHtml(state)` (FTP/Gewicht/Week-TSS uit `state.ftp`/`gewicht`/`weekLoad.tss`; 3a-skin; `#vorm-metrics`). Body herschikt naar export §Vorm: ReadinessCard → LevelCard → niveau-grafiek → MetricRow → conditie-balans (cluster intern ongewijzigd verplaatst; section-kop hernoemd "Trend & details"→"Conditie-balans"). Chart-hex getokeniseerd via `cssColor_(expr)` (Google Charts kent geen CSS-vars → probe-resolve): trend-lijnen → nieuwe `--chart-fitness/fatigue/form` (COMPONENTS-laag, beide token-bestanden), niveau-lijn → `--chart-line`, grid → `--chart-grid`, baseline → `--chart-axis`; `drawVormStrip` banden/strokes/legenda → `--info/good/accent/danger-soft` + `--text-*` via `style="fill:var()"`. Geen nieuwe server-calc → geen gate-case. NB: dot-drempel live = **67/34**.
- **Test-gate — runSelfTest (5f71045):** `src/SelfTest.gs` (COMMITTED). Pure-engine asserts: `tssFromZoneMinutes_`, `checkinDelta_`/`CHECKIN_LEVELS`, `rdyClamp_`, `computeNiveau_` (incl. clamp 0/50 + null), `computeMacroPhase` (Base/Build/Peak/Test + isTestWeek op week-offsets), `getReadinessScore_` factor-dots op de **LIVE drempel 67/34** + missing-factor-rescale (geen harde nul) + band↔score-consistentie (62/48, check-in-robuust), `READINESS_PRESETS`. GEEN side-effects (alleen fixtures + constanten-reads). Uitgesloten want gekoppeld: `bepaalFaseVoorDatum_` (leest Events-tab + Settings; Taper/Recovery-edges). `runSelfTest()` → Logger + `{passed,failed,failures[]}`. NB: dot-drempel **67/34** is live; de "≥70/45" in de readiness-formule-sectie is de oude pré-build-spec.
- **Fase 3c — web-RPE + skip-dispositie (8b2d735):** `saveRpe(dateISO, rpe)` schrijft `rpe_<date>` (1–10) — spiegelt `handleRpeCallback` (DocProps-only, GÉÉN intervals-POST). Client `rpeRatingHtml_` (1–10 knoppen op `actualKaart`, gekozen = `--accent` + `--accent-ring`) → `setRpe`; mismatch → `.rpe-feedback` (`--accent-soft`). `saveDisposition(dateISO, reason∈{geen_tijd,bewust_gerust,iets_anders})` schrijft `disposition_<date>` {reason,ts}; `null` wist (voorstel terug). **GÉÉN generateProposal** bij disponeren (zou via debt "inhalen" + vandaag's voorstel herleven) — read-side overlay: `dashDispositionsByDate_` → dag-status `'gemist'` als dispositie ÉN voorstel ÉN geen actual. Client: affordance "Niet gedaan?" (≤vandaag, gepland, geen actual/rust, niet gedisponeerd), `gemistKaart_` (+ "Terug"), dagstrip-`×`-marker (`--text-muted`).
- **Fase 3b — WeekLoad-kaart (6ff4b47):** `getWeekLoad_(ss, weekStart)` → `state.weekLoad {tss, uren, dagen, geplandTss, progressPct, stale}`. DONE tss/uren/dagen uit Activiteiten-tab (cycling, venster [weekStart,+7d); idx1 Type / idx3 Duur / idx8 TSS), noemer = Σ `weekplan_<maandag>` entry.tss. `stale=false` (F.3-signaal bestaat nog niet, TODO). Client `renderWeekLoad(iconState)` op mount `#week-load` (Index.html, tussen plan-kaart en dagstrip): overline + icon-refresh (idle ↻ / busy spin / done ✓), stat-rij + progress-bar (`--accent-grad`) + stale-banner. Refresh → nieuwe web-fn `refreshWeek()` = `syncActivities()` + verse state (lichter dan regenerateWeb, géén herplanning).
- **Fase 3a — Schema dag-detail skin + opruiming (8c05a1f):** workout-kaart naar skin (`voorstelKaart`/`actualKaart` → `.wk-*` classes; `zoneBar(seg, mini)` kleurt via zone-tokens `--zone-1…6` op `bucket`, niet meer server-hex; multi-sessie → sunken `.wk-sub` sub-kaarten met `wk-minibar`). VERWIJDERD uit `renderDagDetail` 'vandaag'-tak: groene readiness-banner (`v.gereedheid`) + "Garmin-verwachting"-regel (`v.garminStatus.verdict`) — readiness leeft op Vorm. Knoppen: `.act-btn-2` (Garmin) teal→`--btn-secondary-*`, `.act-btn` (Regenereer) → `--btn-primary-*`. NIET toegevoegd: "Doe iets anders"-knop (WorkoutPicker = future, geen dode knop); IF-metric weggelaten (niet in model). Plan-kaart + dagstrip ongemoeid.
- **Fase 2-kern — ochtend-check-in + readiness-bijstelling (a5d0b43):** `saveCheckin(slaap,benen,stress)` schrijft DocProp `checkin_<dISO>` (JSON {slaap,benen,stress,ts}) → herberekende readiness (zelfde shape). `getTodayCheckin_`/`checkinDelta_`/`checkinSummary_`; `CHECKIN_LEVELS` ±2/0 per vraag (slaap slecht/oké/goed · benen zwaar/oké/fris · stress hoog/normaal/laag), somdelta −6..+6 geklemd op de objectieve base-score (GEEN herweging, GEEN 5e factor). Client: `checkinSheetHtml` (overlay sheet via openDrawer, segmented controls, default neutraal), auto-open bij `checkinDone:false` (1×/load, dismissbaar), ingevuld-staat `checkinDoneHtml_` (summary + ronde +-knop + effect-callout). `state.readiness` += `checkinDone/checkinDelta/checkinSummary/checkin`.
- **Fase 1b — Vorm ReadinessCard (6e1b7a9):** `getReadinessScore_(fs,wellness,reeks)` (Algorithm.gs) → `state.readiness {score,band,factors[],chips[]}`, objectief preset `READINESS_PRESETS` (0.30/0.30/0.25/0.15). Normalisatie: vorm-trend TSB −30→+10 + richting-nudge ±10; belasting ATL/CTL 1.5→0.8 (0.6) ⊕ ramp 10→0 (0.4); HRV deficit% −15→+5; slaap 5u→8u. Banden ≥62/48; dot ≥67/34. Client `readinessCardHtml`+`readinessRing_(score,colorVar)` vervangt `renderVormStatus`-mount `#vorm-status`; `.rc-*` CSS. Niveau-block uit Vorm-top (LevelCard = Fase 3).
- **Fase 1a — Schema plan-kaart (8c774da server / 5ff0219 client):** buildPlanModel_→state.plan; planCardHtml (collapsible) vervangt deck-mount op Schema. Detail: zie "Nieuwe contracten".
- **Fase 0b — 4-tab-shell + overlay (3fb917f / ad31aa9):** Trainingen/Niveau containers, switchTab 4-tabs; window.openDrawer/closeDrawer (sheet/drawer) via src/Drawer.html.
- **Fase 0a — dark skin-flip (48ba640):** src/Tokens.html + src/Alias.html, IBM Plex, ring→tokens.
- v2b-B multi-session (2f5a645 feat, 7f6c9a6 docs): per-sessie proposal-keys — base = proposal_<dISO> (s1), extra = proposal_<dISO>_s<n> voor n>=2. external_id: s1 = coach_<dateISO>_ride, n>=2 = coach_<dateISO>_ride_s<n>. Distinct start_date_local (s1 07:00, last 17:00; n>=3 12/19/06h). Single-session events ook verschoven 00:00->07:00 (idempotent via ongewijzigd external_id). Settings PENDEL_DUUR (rij 52, default 80) + PENDEL_AANTAL (rij 53, default 2). Key-format leeft UITSLUITEND in readDaySessions_/writeDaySessions_/deleteDaySessions_ (Algorithm.gs). computeWeekVolumeMin_ sommeert sessies.
- Asymmetrische pendel-intensiteit (9af34fc): sessies 0..N-2 geforceerd pendel_z2; laatste = d.voorgesteldType. genericPendelZ2(mins, settings, mesoWeek, macroFase); recovery-predikaat mesoWeek===4 || macroFase==='Recovery' maakt de "recovery week"-tekst conditioneel.
- Per-sessie kaarten (eab1591/7f076dc/7ee851f): sessies[]-entries {naam, totaalMin, tss, intent, eindopmerking}. dashDayCard_ → voorstel.sessies bij length>1; voorstelKaart → N kaarten via sessieKaart_.
- TRIP-EVENT key-type (HEAD 1e72d6d): trigger isTripEvent (type==='trip'), Build/Peak only. Free-day → 'long_z2' (na climbTypeWorkout_, vóór doel-tak); commute → token 'pendel_trip_intervals' → genericPendelIntervals sweet-spot/tempo. DEMOTE_MAP += pendel_trip_intervals→pendel_z2.
- ITEM C zone-gewogen tss + variant endurance-fill (HEAD 5efd8a6): tssFromZoneMinutes_({low,high,anaerobic}) = round(low*0.7+high*0.95+anaerobic*1.05) = ENIGE rate-bron. renderVariant_ endurance-fill (gap>=5 → Z2-blok, telt als low). genericLongZ2/genericPendelIntervals zone-gewogen. IF daalt nu met duur. CALIBRATIE-NOTITIE: pendel-werkMin VO2max=14@anaerobic < FTP=28@high — eerste tweak-kandidaat, geen blocker.
- [#3] mode-object SERVER-side exposed (commit 381f68d): top-level `mode { eventDriven, macroPhase (Base/Build/Peak/Taper/Recovery), seasonMode (build/maintain), weeksToEvent }` in getDashboardState, uit `bepaalFaseVoorDatum_` (`macro.fase`) + `settings.fase`. LET OP: de CLIENT leest dit NIET (zie [#3]-correctie hierboven); read- én write-side client = toekomst.

### Eerdere milestones
- v2a (521ed24): beschikbaarheid-write-pad — saveAvailability schrijft Weekplanner A3:H9, returnt vers getDashboardState → onState.
- v2b-A (d680037): in-app regenerate + push. pushWeb() = UITZONDERING: returnt {pushedCount,skipped,errors}; onPushResult re-rendert NIET via onState.
- v2c (22c5a30): per-dag rationale (reden) in voorstelKaart, onderdrukt zodra er een actual is.
- v2d (96c57a3): runs = vermoeidheid, niet cycling-fitness — gates op rollingZoneCoverage (r[1]) + computeZoneDebt_ (a.type); recentHardDayDate_ ONGEMOEID (run-inclusief).

### Bestaande stabiele features
Schema-tab: swipe-deck (ring+verdict / niveau-blok). Vorm-tab: niveau-over-tijd grafiek (server dashNiveauReeks_ → payload vorm.niveauReeks, client drawNiveauChart). niveau = clamp(niveauBasis + conditieMod, 0, 50); niveauBasis = computeNiveau_(ftp, gewicht).

### Engine-gedrag (geverifieerd)
- generateProposal: geen args; draait ensureDataAndReconcile_ → syncAll; leest readPlanner live; her-plant alleen tePlannen (train && !gedaan && datum >= vandaag); voltooid behoudt voorgesteldType; schrijft per dag 1..N sessies proposal_<dISO>[_s<n>] + weekplan-aggregaat. Geen auto-regen — alleen menu (Code.gs:44) of web-knop (WebApp.gs:604).
- assignWorkouts: typekeuze per dag uit dagtype + fase + debt/dekking + wellness; minuten schalen alleen de duur, niet het type.
- avoid-consecutive-hard: alleen dag N-1 (calendar), downgradet hard → long_z2; geen N+1-vooruitblik; geen rust-INVOEGING op load.
- recentHardDayDate_ (Algorithm.gs:230): leest Activiteiten-tab actuals, hard op IF >= 0,85 — incl. ongeplande ritten en runs.
- dekking (rollingZoneCoverage, 7d): actuals-bewust; alleen CYCLING_TYPES.
- debt (computeZoneDebt_): alleen dagen met train && gedaan; alleen CYCLING_TYPES. ASYMMETRIE (open): ongeplande ritten zitten wel in dekking, NIET in debt.

### Data & sleutel-functies
Actuals in Activiteiten-tab via syncActivities ← getActivities (intervals.icu; GEEN sport-filter in de sync; Type = idx1 / r[1] / a.type). ACT_HEADERS = 15: Datum idx0, IF idx7 (kolom 8), TSS idx8 (kolom 9), FTP idx12, Gewicht idx13, Rolling FTP idx14. ACT_HISTORY_DAYS = 730. CYCLING_TYPES (Algorithm.gs:42) = Ride/VirtualRide/GravelRide/MountainBikeRide.
Push: pushAllPending_ core / pushAllPendingWorkouts wrapper (Sync.gs); pushEvents_ → POST /events/bulk?upsert=true; external_id = coach_<dateISO>_ride (s1), n>=2 = ..._s<n> (IntervalsApi.gs). Settings: readSettings/loadSettingValue + sheet 'Instellingen' (SETTINGS_SHEET). DAGTYPE_OPTIONS = pendel/vrij/weekend/recovery. Weekplanner A3:H9 (readPlanner): A=Train? D=Minuten E=Dagtype F=Toelichting H=Gedaan?.

### Code-invarianten (engine — overleven de restyle)
- Multi-session: proposal-key-format UITSLUITEND via readDaySessions_/writeDaySessions_/deleteDaySessions_. external_id s1 kaal, n>=2 suffix _s<n>; upsert=true keyed op external_id.
- Pendel-compositie: eerste N-1 sessies = pendel_z2, laatste = d.voorgesteldType.
- sessies[]-shape {naam, totaalMin, tss, intent, eindopmerking}; reden is dag-niveau (d.reden), niet per sessie.
- TRIP: keyIntensity-volgorde Taper → Recovery → climbTypeWorkout_ (Build/Peak) → trip (long_z2) → doel-tak; trip-tak NOOIT vóór Recovery of climb. Pendel-recovery via DEMOTE_MAP (nieuw pendel-key-type MOET DEMOTE_MAP-entry → pendel_z2). workoutZones: pendel_trip_intervals gepind ['low','high'] (1309); pendel_z2 gepind ['low'] (1303). Twee dag-smaken: pendel-dag → token-pad (731), normale dag → keyIntensity (765).
- TSS: tss ALTIJD via tssFromZoneMinutes_; per-zone-minuten uit bestaande intent (renderVariant_/genericLongZ2:1860), warm+cool al in intent.low (niet opnieuw invouwen). Begrensde key-set: extra duur → Z2 (fill-floor 5 min), NOOIT meer reps.
- Opruim-kandidaat (niet urgent): vorm.macroFase (WebApp.gs:476) overlapt met top-level mode.macroPhase → out-of-sync-risico.

## Durabele lessen
Zie CLAUDE.md. Kort: visueel verifieren op /dev (incognito + hard refresh); write-pad-patroon = google.script.run → serverfn returnt vers getDashboardState → onState (behalve pushWeb); NL-locale-formules bij setFormula (komma decimaal + puntkomma separator) — nu n.v.t. (writes zijn waarden/JSON), relevant zodra een write formules raakt; STAP 0-recon + 200-woorden rapport-cap.
