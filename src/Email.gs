/**
 * Email.gs — Dagelijkse email digest.
 *
 * Stuurt elke ochtend een korte samenvatting per email: de training
 * voor vandaag, de huidige form/fitness-status en eventuele waarschuwingen.
 */

function sendDailyDigest() {
  // Stub — implementatie komt in volgende stap.
}
