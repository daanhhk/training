/**
 * Sync.gs — Dagelijkse sync trigger.
 *
 * Time-based trigger die elke dag intervals.icu data ophaalt, de
 * fitness-curve bijwerkt en zo nodig het weekvoorstel herberekent.
 */

function dailySync() {
  // Stub — implementatie komt in volgende stap.
}
