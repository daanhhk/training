/**
 * Zones.gs — Tab "Zones".
 *
 * Berekent automatisch de power-, heart-rate- en pace-zones op basis
 * van de FTP/LTHR uit Instellingen en toont ze in een leesbare tabel.
 */

function buildZonesSheet() {
  // Stub — implementatie komt in volgende stap.
}
