/**
 * Settings.gs — Tab "Instellingen".
 *
 * Bouwt en beheert het Instellingen-tabblad waar de gebruiker zijn
 * profiel invult (naam, FTP, gewicht, intervals.icu API key, doelen,
 * beschikbare trainingsdagen, etc.).
 */

function buildSettingsSheet() {
  // Stub — implementatie komt in volgende stap.
}
