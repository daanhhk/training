/**
 * Algorithm.gs — Workout toewijzing en opbouw.
 *
 * Kern van de coach: bepaalt op basis van fitness/fatigue/form (CTL/ATL/TSB)
 * welke workout-type op welke dag past om in Garmin's "Productive" status
 * te blijven, en bouwt de concrete workout-structuur (intervals, duur, IF).
 */

function assignWorkouts() {
  // Stub — implementatie komt in volgende stap.
}

function buildWorkout(/* type, durationMin, targetTss */) {
  // Stub — implementatie komt in volgende stap.
}
