/**
 * Planner.gs — Tab "Weekplanner".
 *
 * Bouwt het weekoverzicht: per dag een rij met geplande training,
 * load-targets en ruimte voor handmatige aanpassingen door de gebruiker.
 */

function buildPlannerSheet() {
  // Stub — implementatie komt in volgende stap.
}
