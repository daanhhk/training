/**
 * IntervalsApi.gs — intervals.icu API laag.
 *
 * Wrapper rond de intervals.icu REST API: ophalen van activiteiten,
 * wellness-data, fitness-curve (CTL/ATL/TSB) en pushen van geplande
 * workouts naar de kalender.
 */

function intervalsFetch(/* endpoint, params */) {
  // Stub — implementatie komt in volgende stap.
}
